# TASKS — NG Kütahya Seramik Yönetim Paneli

> Tamamlanan tasklar silinmez. Bekleyen tasklar da burada tutulur. Geçersiz kılınan kayıtlar iz bırakılarak işaretlenir.

---

## Faz 0 — Analiz

- [x] **Task 0.1** — Kavramsal Analiz Çıkarımı (17.07.2026, ilk sürüm) — **Durum: Düzeltildi (17.07.2026, ikinci geçiş).** Hatalar: modül sayısı ("14" yanlıştı, doğrusu 15), "onay akışı" ifadesinin gereksinim gibi sunulması (geçersiz kılındı), SEO sıralama mantığı, güvenlik ayrımı. Detaylar PROJECT_MEMORY.md "Düzeltme Günlüğü"nde.
- [x] **Task 0.1 — İkinci Geçiş (Düzeltilmiş)** (17.07.2026) — Modül sayısı 15 olarak doğrulandı, doküman-gereksinimi/teknik-çıkarım/açık-karar ayrımı yapıldı, güvenlik gereksinimleri ikiye bölündü, SEO sıralaması düzeltildi, kapsam ifadesi PROJECT_MEMORY.md'ye netleştirildi. **Durum: Kullanıcı tarafından onaylandı (17.07.2026).**
- [x] **Task 0.2** — Açık teknik kararların tek tek değerlendirilmesi — **TAMAMLANDI (17.07.2026).** Tüm 10 karar onaylandı, kod/solution/proje dosyası üretilmedi. Alt kararlar:
  1. [x] Mevcut proje teslimatının kesin teknik sınırı — **Onaylandı (17.07.2026, Seçenek A — bkz. ARCHITECTURE_DECISIONS.md ADR-002).**
  2. [x] ASP.NET Core uygulama tipi ve panelin sunum modeli — **Onaylandı (17.07.2026, server-rendered MVC — bkz. ARCHITECTURE_DECISIONS.md ADR-003).**
  3. [x] EF Core Code First / Database First — **Onaylandı (17.07.2026, Code First — bkz. ARCHITECTURE_DECISIONS.md ADR-004).**
  4. [x] Yönetim paneli authentication yöntemi — **Onaylandı (17.07.2026, ASP.NET Core Identity + Cookie — bkz. ARCHITECTURE_DECISIONS.md ADR-005). 2FA detayları ve şifre sıfırlama akışı bilinçli olarak açık bırakıldı.**
  5. [x] Dosya ve görsel saklama yaklaşımı — **Onaylandı (17.07.2026, Yerel Dosya Sistemi + storage abstraction — bkz. ARCHITECTURE_DECISIONS.md ADR-006). İsimlendirme standardı ve WebP/AVIF/CDN alt kararı açık bırakıldı.**
  6. [x] Çoklu dil veri modeli ve fallback davranışı — **Onaylandı (17.07.2026, veri modeli: merkezi Translations/A1 — bkz. ARCHITECTURE_DECISIONS.md ADR-007). Fallback davranışı "Gelecek Faz/Karar Bekleniyor" durumunda.**
  7. [x] Showroom ayrı modül mü, Bayi kategorisi mi — **Onaylandı (17.07.2026, tek Dealer entity + Category ayrımı — bkz. ARCHITECTURE_DECISIONS.md ADR-008).**
  8. [x] Public site ile gelecekteki entegrasyon sınırı — **Onaylandı (17.07.2026, ek iskelet yok, katman ayrımı yeterli — bkz. ARCHITECTURE_DECISIONS.md ADR-009).**
  9. [x] SAP entegrasyonunun Faz 1/Faz 2 konumu — **Onaylandı (17.07.2026, tamamen Faz 2 — bkz. ARCHITECTURE_DECISIONS.md ADR-010).**
  10. [x] Güvenlik gereksinimlerinin teknik karşılıkları — **Onaylandı (17.07.2026, Rate Limiting=yerleşik, Loglama=ILogger+Serilog, Dosya Doğrulama=MIME+magic-byte+whitelist — bkz. ARCHITECTURE_DECISIONS.md ADR-011).**
  - **Kural:** Kararlar tek tek ele alınır, ARCHITECTURE_DECISIONS.md'ye sadece kesinleşen karar ADR olarak eklenir. Bir karara geçmeden önceki kararın kullanıcı onayı alınmış olmalı.

- [x] **Task 1.1** — Solution ve Katman İskeleti Hazırlığı — **TAMAMLANDI (17.07.2026).**
  - Solution (`NGKutahyaSeramik_AdminPanel.sln`) + 4 proje (Presentation/Application/Domain/Infrastructure) oluşturuldu.
  - Onaylanan reference graph kuruldu: Presentation→(Application, Infrastructure); Application→Domain; Infrastructure→(Application, Domain); Domain→(yok). Döngüsel bağımlılık yok.
  - `AddApplicationServices` (Application) ve `AddInfrastructureServices` (Infrastructure) DI extension'ları boş gövdeli olarak oluşturuldu — gerçek servis kaydı yok.
  - Presentation'a Serilog.AspNetCore + Serilog.Sinks.Console eklendi, Program.cs'e yalnızca console sink ile minimal Serilog bootstrap kuruldu.
  - Program.cs: Serilog bootstrap, AddControllersWithViews, AddApplicationServices, AddInfrastructureServices, ortama göre exception handling, HSTS, HTTPS redirection, static files, routing, default route, Run — Authentication/Authorization/RateLimiter middleware'leri **eklenmedi** (henüz altyapıları kurulmadı).
  - `dotnet restore` ✅, `dotnet build` ✅ (0 hata/0 uyarı), `dotnet run` ✅ (unhandled exception yok, `GET /` → 404 beklenen davranış).
  - Derleme sırasında gerçekten gerekli olduğu doğrulanan ek paketler: `Microsoft.Extensions.DependencyInjection.Abstractions` (Application, Infrastructure), `Microsoft.Extensions.Configuration.Abstractions` (Infrastructure) — plain class library ASP.NET Core shared framework'ünü örtük almadığı için.
  - **Bilinçli olarak yapılmayanlar:** Entity/DbContext/migration, Identity kullanıcı sınıfı/`AddIdentity`/Cookie kesinleştirme, repository/service/business logic, Controller/action/View içeriği, storage abstraction interface'i, EF Core/Identity paketlerinin kurulumu, Authentication/Authorization/RateLimiter middleware kaydı, gerçek rate limit değerleri, log hedefleri (dosya/SQL/Seq).
  - **Task 1.2 için planlanan bağımlılıklar (henüz kurulmadı):** `Microsoft.EntityFrameworkCore`, `Microsoft.EntityFrameworkCore.SqlServer`, `Microsoft.EntityFrameworkCore.Tools`, `Microsoft.EntityFrameworkCore.Design`, `Microsoft.AspNetCore.Identity.EntityFrameworkCore` — DbContext ve Identity kullanıcı modeliyle birlikte Task 1.2'de kurulacak.
  - ARCHITECTURE_DECISIONS.md'ye yeni ADR eklenmedi, mevcut ADR'ler değiştirilmedi (bu task saf uygulama task'ıydı, mimari karar değil).

- [x] **Task 1.2A** — Identity ve DbContext Altyapısı — **TAMAMLANDI (17.07.2026).**
  - `Infrastructure/Identity/ApplicationUser.cs` — `IdentityUser`'dan türeyen, ek property yok.
  - `Infrastructure/Identity/ApplicationRoles.cs` — 4 rol için sabit string (enum değil): Admin, İçerik Editörü, SEO Editörü, Ürün Yöneticisi.
  - `Infrastructure/Persistence/AppDbContext.cs` — `IdentityDbContext<ApplicationUser>`, domain `DbSet` yok, `OnModelCreating` yok.
  - `Infrastructure/DependencyInjection.cs` — `AddDbContext<AppDbContext>` (UseSqlServer + connection string guard clause) + `AddIdentity<ApplicationUser, IdentityRole>().AddEntityFrameworkStores<AppDbContext>().AddDefaultTokenProviders()` — Identity varsayılan parola/lockout ayarları değiştirilmedi.
  - `Presentation/Program.cs` — `ConfigureApplicationCookie` (LoginPath, AccessDeniedPath, HttpOnly, SecurePolicy=Always, SameSite=Lax; ExpireTimeSpan/SlidingExpiration'a dokunulmadı — Identity varsayılanları korunuyor), `AddAuthorization()` (bare), `UseAuthentication()`/`UseAuthorization()` (Routing sonrası, MapControllerRoute'tan önce).
  - Paketler (9.0.18): Infrastructure → `Microsoft.EntityFrameworkCore`, `Microsoft.EntityFrameworkCore.SqlServer`, `Microsoft.AspNetCore.Identity.EntityFrameworkCore` + `<FrameworkReference Include="Microsoft.AspNetCore.App" />` (derleme hatası nedeniyle gerekli olduğu doğrulandı — `AddIdentity` ASP.NET Core paylaşılan çatısının parçası). Presentation → `Microsoft.EntityFrameworkCore.Design` (CLI migration akışı için; `Tools` paketi eklenmedi).
  - Connection string: `appsettings.json` → boş placeholder; `appsettings.Development.json` → `.\SQLEXPRESS` (bu makinede çalıştığı doğrulanan instance, Trusted Connection, kimlik bilgisi yok).
  - Doğrulama: `dotnet restore` ✅, `dotnet build` ✅ (0/0), `dotnet run` ✅ (unhandled exception yok, DB erişimi yok), `GET /` → 404 (beklenen).
  - **Bilinçli olarak yapılmayanlar:** Migration, `dotnet ef database update`, veritabanı oluşturma, Role/Admin seed, Controller/View, Login/Register, User/Role CRUD, 2FA, şifre sıfırlama, JWT, domain entity, `DbSet`, generic repository, BaseEntity, audit/soft-delete, rate limiting, SAP, public API, Identity.UI, `AddDefaultUI`, session, CORS, antiforgery için özel global filtre.
  - ARCHITECTURE_DECISIONS.md'ye dokunulmadı — yeni ADR eklenmedi, mevcut ADR'ler değiştirilmedi.

- [x] **Task 1.2B** — İlk Migration, Veritabanı Oluşturma ve Identity Seed — **TAMAMLANDI (18.07.2026).**
  - Design-time DbContext doğrulaması: `dotnet ef dbcontext info` mevcut global `dotnet-ef 10.0.9` ile başarılı çalıştı → repo-yerel `9.0.18` aracı kurulumu **gerekmedi** (kullanıcı onayı: önce global ile dene, sorun çıkarsa yeniden onay iste — sorun çıkmadı).
  - `InitialIdentity` migration'ı oluşturuldu (`Infrastructure/Persistence/Migrations/`) — yalnızca 7 Identity tablosu, domain tablosu yok (kod incelenerek doğrulandı).
  - `dotnet ef database update` ile `NGKutahyaSeramikAdminPanel` veritabanı `.\SQLEXPRESS` üzerinde oluşturuldu ve migration uygulandı (sqlcmd ile 8 tablo — 7 Identity + `__EFMigrationsHistory` — doğrulandı).
  - `Infrastructure/Identity/IdentitySeeder.cs` — rol seed (koşulsuz, `RoleExistsAsync`/`CreateAsync`, `ApplicationRoles` sabitleriyle, idempotent) + admin seed (`SeedAdmin:Email`/`Password` User Secrets'tan, eksikse WARNING+skip, `FindByEmailAsync`/`IsInRoleAsync` ile idempotent).
  - `Presentation/Program.cs` — seed çağrısı `IServiceScope` içinde, `app.Build()` sonrası/`app.Run()` öncesi, try/catch+log+rethrow ile.
  - `dotnet user-secrets init` (Presentation) — `UserSecretsId` eklendi, **hiçbir değer girilmedi** (gerçek admin credential kullanıcı tarafından sağlanmadı, sahte değer üretilmedi — talimat gereği).
  - `Database.MigrateAsync()` kodda **kullanılmadı** — migration/database update yalnızca CLI ile manuel yapıldı (ADR-004'teki açık karar kasıtlı olarak kapatılmadı).
  - **Doğrulanan sonuç:** `AspNetRoles` → 4 rol (Admin, İçerik Editörü, SEO Editörü, Ürün Yöneticisi). `AspNetUsers` → **0 satır — admin kullanıcı henüz oluşturulmadı** (gerçek credential eksik, WARNING logu doğrulandı, uygulama çökmedi).
  - **Bilinçli olarak yapılmayanlar:** AccountController, Login/AccessDenied View, UI üzerinden auth testi, `ApplicationUser`'a yeni property, yeni rol, enum, `EmailConfirmed=true` ataması (framework varsayımında bırakıldı — `false`), `Microsoft.EntityFrameworkCore.Tools`, repo-yerel `dotnet-ef` aracı (gerekmedi).
  - ARCHITECTURE_DECISIONS.md'ye dokunulmadı — yeni ADR eklenmedi, mevcut ADR'ler değiştirilmedi.

- [x] **Task 1.2C** — İlk Admin Kullanıcısının Seed Edilmesi ve İdempotency Doğrulaması — **TAMAMLANDI (18.07.2026).**
  - Kullanıcı tarafından sağlanan gerçek development admin e-postası/parolası `dotnet user-secrets set` ile Presentation projesinin User Secrets deposuna kaydedildi (`SeedAdmin:Email`, `SeedAdmin:Password`) — appsettings.json/appsettings.Development.json/kaynak koda hiçbir değer yazılmadı (grep ile doğrulandı).
  - 1. çalıştırma: Admin kullanıcı `AspNetUsers`'da oluştu, `Admin` rolüne bağlandı, `EmailConfirmed` framework varsayımında (`false`) kaldı.
  - 2. çalıştırma: Hiçbir `INSERT` yok (yalnızca `SELECT`) — duplicate kullanıcı/rol ilişkisi oluşmadı; `PasswordHash`/`SecurityStamp`/`ConcurrencyStamp` checksum'ları birebir aynı kaldı (parola overwrite edilmedi).
  - `dotnet build` → 0 Warning / 0 Error.
  - PasswordHash/SecurityStamp/ConcurrencyStamp/User Secrets değerleri hiçbir log/rapor/dosyada açık gösterilmedi — yalnızca checksum karşılaştırması kullanıldı.
  - ARCHITECTURE_DECISIONS.md'ye dokunulmadı.

- [x] **Kapanış Doğrulaması** — **TAMAMLANDI (18.07.2026).** `AspNetUsers`'daki admin kullanıcısının Email/UserName/EmailConfirmed alanları ve `AspNetUserRoles`⋈`AspNetRoles` join'i ile yalnızca `Admin` rolüne sahip olduğu (toplam rol sayısı=1, başka rol yok) salt okunur SQL sorgularıyla doğrulandı. Dosya değişikliği/migration/seed yapılmadı.

## ✅ TASK 1 (IDENTITY FOUNDATION) — TAMAMEN TAMAMLANDI (18.07.2026)

Kapsadığı alt task'lar: 1.1, 1.2A, 1.2B, 1.2C ve yukarıdaki kapanış doğrulaması — hepsi tamamlandı ve onaylandı.

---

## Faz 2 — Authentication UI

- [x] **Task 2.1** — Authentication UI (Login/Logout/AccessDenied/Dashboard) — **TAMAMLANDI (18.07.2026).**
  - `AccountController` (Login GET/POST, Logout POST, AccessDenied GET), `HomeController` (`[Authorize]`, Index), `LoginViewModel`, `_Layout.cshtml` (minimal), `Login.cshtml`, `AccessDenied.cshtml`, `Home/Index.cshtml` oluşturuldu.
  - **Kritik düzeltme:** `Program.cs`'te eksik olan `app.UseAuthentication()`/`app.UseAuthorization()` middleware'leri eklendi (Task 1.2A'dan kalan, `[Authorize]` için zorunlu bir eksiklik — servisler kayıtlıydı, middleware yoktu).
  - Açık redirect koruması (`Url.IsLocalUrl`), CSRF (`[ValidateAntiForgeryToken]`), enumeration koruması (tek genel hata mesajı), lockout (`lockoutOnFailure:true`), Logout yalnızca POST — hepsi plana uygun uygulandı.
  - 11 doğrulama senaryosunun tamamı gerçek seed admin credential'ıyla test edildi ve geçti (bkz. PROJECT_MEMORY.md "Task 2.1" detay bölümü) — `dotnet build` 0/0 dahil.
  - Infrastructure/Domain/Application'a dokunulmadı, yeni paket eklenmedi, migration/seed çalıştırılmadı, ARCHITECTURE_DECISIONS.md değiştirilmedi.
  - **Bilinçli olarak yapılmayanlar:** Kullanıcı kayıt, şifre sıfırlama, 2FA, kullanıcı/rol yönetim ekranları, rol-bazlı gerçek `[Authorize(Roles=...)]`, RememberMe, kurumsal görsel tasarım, geçici test controller'ı (AccessDenied organik tetiklemesi Task 2.2'ye bırakıldı).

- [x] **Task 2.2** — Role-Based Authorization — **TAMAMLANDI (18.07.2026).**
  - `HomeController.AdminOnly()` (`[Authorize(Roles = ApplicationRoles.Admin)]`) + `Views/Home/AdminOnly.cshtml` (minimal, iş mantığı yok) eklendi.
  - `IdentitySeeder`'a `SeedDevelopmentTestUserAsync` eklendi — İçerik Editörü rolünde, idempotent, admin seed deseniyle birebir; `Program.cs`'te yalnızca `if (app.Environment.IsDevelopment())` altında çağrılıyor (environment kontrolü Infrastructure'a taşınmadı).
  - `SeedTestUser:Email`/`Password` User Secrets'a kaydedildi (`editor@localhost`) — hiçbir dosyaya yazılmadı.
  - Named policy oluşturulmadı, doğrudan `[Authorize(Roles=...)]` kullanıldı; `ApplicationRoles.cs` değiştirilmedi.
  - 11 doğrulama senaryosunun tamamı geçti (bkz. PROJECT_MEMORY.md "Task 2.2" detay bölümü) — organik AccessDenied tetiklemesi, idempotency, parola/checksum korunumu, **Production ortamı simülasyonuyla test kullanıcı seed'inin çağrılmadığının ampirik kanıtı** dahil.
  - Rol değişikliği/cookie davranışı **belgelendi, manuel DB testi yapılmadı** (kullanıcının kararıyla — gereksiz risk).
  - Infrastructure değişikliği yalnızca `IdentitySeeder` ile sınırlı kaldı; Domain/Application'a dokunulmadı, yeni paket eklenmedi, migration/seed komutu çalıştırılmadı, ARCHITECTURE_DECISIONS.md değiştirilmedi.

- [x] **Task 3.1A** — Translation (Çoklu Dil) Şema Kararları ve Mimari Analizi — **TAMAMLANDI (19.07.2026).**
  - Yalnızca analiz/karar — hiçbir entity/DbContext/configuration/migration yazılmadı, kod değişikliği yok.
  - **A — FieldName:** Düz `nvarchar`, DB constraint yok, kod tarafında const string sabitler.
  - **B — Value:** Tek `nvarchar(max)`, kısa/uzun tüm alanlar aynı sütunda.
  - **C — Satır yapısı:** Her (EntityType, EntityId, LanguageId, FieldName) ayrı satır; JSON yok.
  - **D — EntityType:** Enum, Infrastructure'da açık bir `ValueConverter<EntityType,string>` ile string'e çevrilir (`enum.ToString()`'e bağımlı değil); eksik mapping'de exception, sessiz fallback yok.
  - **E — Unique index:** `(EntityType, EntityId, LanguageId, FieldName)`.
  - **F — Yetim kayıt temizliği:** Trigger/background job yok; entity silme işlemiyle aynı transaction'da Application katmanı servisi yönetir.
  - **G — Language ilişkisi:** Ayrı `Language` entity'si (`Id`, `Code`, `Name`, `IsActive`, `DisplayOrder`); `Translation.LanguageId` → `Language.Id` klasik FK (surrogate, doğal anahtar değil); `Language.Code` ayrı unique index; `Translation`'da `LanguageCode` sütunu tutulmaz; `ON DELETE RESTRICT`; dil silinmez, `IsActive=false` ile pasif yapılır.
  - **Migration kararı:** Language + Translation Task 3.1B'de **tek migration**da birlikte oluşturulacak; dil verisi (TR/EN/DE/FR/ES/AR/RU) migration'a gömülmez, `IdentitySeeder` deseniyle ayrı bir seeder ile eklenecek.
  - **ADR-012** olarak ARCHITECTURE_DECISIONS.md'ye eklendi — ADR-007 değiştirilmedi/geçersiz kılınmadı, yalnızca detaylandırıldı.
  - Backlog madde #2 ("Kullanıcı/Rol Yönetimi") bu task kapsamında "Kısmi Tamamlandı: RBAC altyapısı tamamlandı, CRUD yönetim ekranları ileride yapılacak" ifadesiyle güncellendi — CRUD ekranları yapılmadığı için `[ ]` olarak kaldı, tamamen kapatılmadı.
  - **Bilinçli olarak yapılmayanlar:** Domain entity, Infrastructure configuration, `AppDbContext` değişikliği, migration, `dotnet ef database update`, seeder — hepsi Task 3.1B'ye bırakıldı.

- [x] **Task 3.1B** — Translation & Language Infrastructure Implementation — **TAMAMLANDI (19.07.2026).**
  - **Domain:** `Domain/Enums/EntityType.cs` (9 üye: Product, Category, Collection, Blog, News, Page, Banner, ReferenceProject, Dealer), `Domain/Entities/Language.cs`, `Domain/Entities/Translation.cs` — sade POCO, private setter + constructor guard (Code/Name/FieldName boş-whitespace kontrolü, normalizasyon yok — bu Application katmanına bırakıldı), `Language.Translations` navigation collection eklendi.
  - **Infrastructure:** `Persistence/Conversions/EntityTypeMapping.cs` (tek doğruluk kaynağı statik dizi) + `EntityTypeConverter.cs` (`ValueConverter<EntityType,string>`, `enum.ToString()`'e bağımlı değil); `Persistence/Configurations/LanguageConfiguration.cs` + `TranslationConfiguration.cs` (Fluent API — unique index'ler, `DeleteBehavior.Restrict`, `WithMany(l => l.Translations)`); `AppDbContext`'e `DbSet<Language>`/`DbSet<Translation>` + ilk `OnModelCreating` override (`base.OnModelCreating` + `ApplyConfigurationsFromAssembly`); `Persistence/LanguageSeeder.cs` (tüm ortamlarda çalışır, idempotent, TR/EN/DE/FR/ES/AR/RU).
  - **Migration:** `AddTranslationInfrastructure` (tek migration, Language+Translation birlikte) — `InitialIdentity`'ye dokunulmadı.
  - **Doğrulama:** 11/11 senaryo geçti — tablo şeması, 7 dil doğru sırada, 3 kez `dotnet run` ile idempotency (0 duplicate), `Language.Code`/`Translation` composite unique index, geçersiz FK reddi, `ON DELETE RESTRICT` (RU dili hiç silinmedi), converter round-trip (`PRODUCT` doğru okundu) + bilinmeyen değerde (`UNKNOWN_XYZ`) açık `InvalidOperationException`, build 0/0.
  - Geçici doğrulama kodu (`--verify-translations` bayrağı) ve geçici test satırları (manuel SQL) doğrulama sonrası tamamen kaldırıldı/temizlendi.
  - **Bilinçli olarak yapılmayanlar:** `ITranslationCleanupService`/orphan-cleanup abstraction'ı, Language CRUD/Application servisi, `Code` normalizer, SEO'nun `EntityType` paylaşımı kararı, ikincil `(EntityType,EntityId)` index'i, audit alanları (`CreatedAt`/`UpdatedAt`) — hiçbiri bu task'ın kapsamında değildi.
  - Backlog madde #3 ("Dil Yönetimi altyapısı") bu task ile **tamamen tamamlandı** işaretlendi (şema kararı + implementasyon ikisi de bitti).

- [x] **Task 4.1** — Kategori Yönetimi (uçtan uca: mimari + implementasyon) — **TAMAMLANDI (18-19.07.2026).**
  - Bu task'tan itibaren çalışma düzeni değişti: plan + implementasyon + migration + doğrulama + dokümantasyon **tek task'ta** birlikte yürütüldü (önceki Task 4.1A'nın saf planlama-only deseninin devamı değil).
  - **Domain:** `Domain/Entities/Category.cs` — sade POCO (`Id`, `ParentCategoryId`, `ParentCategory`, `Children`, `ImagePath`, `DisplayOrder`, `IsActive`), self-referencing, `UpdateDetails`/`Activate`/`Deactivate` metotları. Çevrilebilir sütun yok (Name/Description/SeoUrl/MetaTitle/MetaDescription tamamen Translation'da).
  - **Application (ilk gerçek kullanım):** `Application/Categories/` (`ICategoryRepository`, `CategoryService`, `CategoryDto`, `CategoryRequests`, `CategoryOperationResult`, `CategoryFields`), `Application/Translations/ITranslationService.cs`, `Application/IUnitOfWork.cs`. Application projesi EF Core'a hiç referans vermiyor.
  - **Infrastructure:** `CategoryConfiguration` (self-referencing FK `Restrict`), `CategoryRepository`, `TranslationService`, `UnitOfWork`; `AppDbContext`'e `DbSet<Category>`; DI kayıtları (`ITranslationService`/`ICategoryRepository`/`IUnitOfWork` Infrastructure'da, `CategoryService` Application'da, hepsi scoped).
  - **Presentation:** `CategoryController` (Index/Create/Edit/ToggleActive/Delete), `CategoryFormViewModel`, `Views/Category/{Index,Create,Edit,_CategoryForm}.cshtml`, `_Layout.cshtml`'e nav linki eklendi.
  - **Migration:** `AddCategories` — yalnızca `Categories` tablosu, `InitialIdentity`/`AddTranslationInfrastructure`'a dokunulmadı.
  - **RBAC:** Controller seviyesi (Admin+Ürün Yöneticisi+İçerik Editörü+SEO Editörü görüntüleme), action seviyesi (Admin+Ürün Yöneticisi düzenleme, yalnızca Admin silme) — doğrudan `[Authorize(Roles=...)]`, policy/claim yok.
  - **Doğrulama:** Build 0/0; migration şeması yalnızca beklenen `Categories` tablosunu içeriyor; 10/10 iş kuralı senaryosu geçici doğrulama kodu ile test edildi (ana/alt kategori oluşturma, 3. seviye reddi, self-parent reddi, aynı-parent-aynı-ad reddi, farklı-parent-aynı-ad kabulü, edit+upsert+opsiyonel-alan-silme, toggle, alt-kategorili-silme reddi, leaf silme + Translation temizliği) — tamamı beklenen sonucu verdi; sqlcmd ile şema ve temizlik doğrulandı (0 kalıntı satır); anonim erişim `/Category` ve `/Category/Delete` için Login'e yönlendirme doğrulandı. **Rol-bazlı canlı çoklu-kullanıcı testi bu oturumda yapılamadı** (gerçek kullanıcı parolaları User Secrets'ta, bu oturumda paylaşılmadı) — RBAC mekanizması Task 2.1/2.2'de kanıtlanmış aynı `[Authorize(Roles=...)]` deseniyle kod incelemesiyle doğrulandı.
  - Geçici doğrulama kodu (`--verify-categories` bayrağı) doğrulama sonrası tamamen kaldırıldı.
  - **Küçük, onay istenmeden alınan 2 ek karar:** `IUnitOfWork` (Application'ın EF Core'suz kalması + tek `SaveChangesAsync` kısıtlarını birlikte karşılamak için zorunlu); `ITranslationService.DeleteTranslationFieldAsync` (boşaltılan opsiyonel çeviri alanının silinmesi kuralını karşılamak için gerekli).
  - **Bilinçli olarak yapılmayanlar:** Gerçek dosya yükleme/storage abstraction (ADR-006, ayrı task), arama/filtreleme/sayfalama/toplu işlem/Excel aktarımı, ayrı Details ekranı, drag-drop sıralama, otomatik test projesi, Product/Collection ilişkisi (henüz o entity'ler yok).

- [x] **Task 4.2** — Koleksiyon Yönetimi (uçtan uca) — **TAMAMLANDI (19.07.2026).**
  - **Kritik ilişki kararı (doküman analiziyle çözüldü, onay istenmedi):** `Collection`'ın `Category`'ye FK ile bağlı **olmadığı** 4 bağımsız doküman kanıtıyla belirlendi: (1) Madde 20 Category'yi hiç anmıyor; (2) Madde 36.1 Ana Tablolar'da `Collections` satırının ilişkileri yalnızca "Products, Documents" — Categories yok; (3) Madde 27.2 SEO URL yapısı düz/bağımsız (`/koleksiyonlar/{seo-url}`, kategori segmenti yok); (4) Madde 18.1 Ürün Veri Modeli'nde `Category` ve `SeriesName`/`CollectionName` bağımsız kardeş alanlar. Madde 16.4'teki "Marka > Kategori > Alt Kategori > Koleksiyon > Ürün" ifadesi Bölüm 16'da (public site modülleri) geçiyor — kavramsal gezinme hiyerarşisi, FK şeması değil. **Sonuç:** `Collection` entity'sinde `CategoryId` yok; gerçek ilişki (varsa) Product task'ında ele alınacak.
  - **Domain:** `Domain/Entities/Collection.cs` — sade POCO (`Id`, `ImagePath`, `DisplayOrder`, `IsActive`), FK/hiyerarşi yok, Category'den daha basit.
  - **Application:** `Application/Collections/` (`ICollectionRepository`, `CollectionService`, `CollectionDto`, `CollectionRequests`, `CollectionOperationResult`, `CollectionFields`) — Category'nin deseniyle birebir, ancak parent/hiyerarşi mantığı yok. Mevcut `ITranslationService`/`IUnitOfWork` **aynen tekrar kullanıldı**, hiçbir yeni genel-amaçlı metot eklenmedi.
  - **Infrastructure:** `CollectionConfiguration` (FK yok, gereksiz index yok), `CollectionRepository`; `AppDbContext`'e `DbSet<Collection>`; DI kayıtları.
  - **Presentation:** `CollectionController` (Index/Create/Edit/ToggleActive/Delete — Category dropdown'u yok), `CollectionFormViewModel`, `Views/Collection/{Index,Create,Edit,_CollectionForm}.cshtml` (Index düz liste, tree değil), `_Layout.cshtml`'e nav linki.
  - **Migration:** `AddCollections` — yalnızca `Collections` tablosu, hiçbir FK yok; Identity/Languages/Translations/Categories tablolarına dokunulmadı.
  - **RBAC:** Category ile birebir aynı matris (Admin+Ürün Yöneticisi+İçerik Editörü+SEO Editörü görüntüleme; Admin+Ürün Yöneticisi düzenleme; yalnızca Admin silme).
  - **İş kuralı farkı:** Duplicate isim kontrolü Category'de "aynı parent altında" iken, Collection'da **global** (tüm koleksiyonlar arasında) — çünkü Category ilişkisi yok ve doküman "272 **benzersiz** seri adı" diyerek koleksiyon adının doğal olarak tekil bir kavram olduğunu gösteriyor.
  - **Doğrulama:** Build 0/0; migration yalnızca beklenen `Collections` şemasını içeriyor; 6/6 iş kuralı senaryosu geçici doğrulama koduyla test edildi (oluşturma, TR adı zorunluluğu, global duplicate ad reddi, edit+upsert+opsiyonel-alan-silme, toggle, silme+Translation temizliği) — tamamı beklenen sonucu verdi; sqlcmd ile şema+temizlik+diğer tabloların (`Categories`/`Languages`/`Translations`/Identity) etkilenmediği doğrulandı; anonim erişim (`/Collection`, `/Collection/Create`, `/Collection/Delete`) Login'e yönlendirme ile doğrulandı. **Rol-bazlı canlı çoklu-kullanıcı testi bu oturumda da yapılamadı** (aynı credential kısıtı).
  - Geçici doğrulama kodu (`--verify-collections` bayrağı) doğrulama sonrası tamamen kaldırıldı.
  - **Yeni genel-amaçlı abstraction eklenmedi** — `ITranslationService`/`IUnitOfWork` değişmeden tekrar kullanıldı, `CategoryService`'e spekülatif Collection kontrolü eklenmedi.
  - **Bilinçli olarak yapılmayanlar:** Product/Document ilişkisi (o entity'ler henüz yok — silme sırasında ürün/doküman bağlılığı kontrolü yok), gerçek dosya yükleme, arama/filtreleme/sayfalama/toplu işlem, ayrı Details ekranı, drag-drop sıralama, otomatik test projesi, `CollectionSeeder` (272 gerçek seri adı veri aktarımı task'ının konusu).

- [x] **Task 5** — Ürün Yönetimi (çekirdek CRUD, uçtan uca) — **TAMAMLANDI (19.07.2026).**
  - Yalnızca çekirdek `Product` entity'si (Madde 18.1 veri modeli) kapsandı — kapsam sınırlaması bilinçli, backlog zaten Ürün Görselleri (#8)/Katalog-Doküman (#9)/Referans Proje (#10) maddelerini ayrı tutuyordu.
  - **Kritik ilişki kararı (onay istenmeden, doküman analiziyle çözüldü):** Madde 18.1'in `SeriesName` (Zorunlu) alanı `Product.CollectionId` FK'sine bağlandı (Madde 20: "Her seri bir koleksiyon" + 272 seri = 272 potansiyel Collection kaydı örtüşmesi); ayrı `CollectionName` serbest metin alanı eklenmedi (Collection ilişkisiyle anlamsal çakışma, YAGNI). `Category` alanı da `Product.CategoryId` FK'sine bağlandı. Product hem `CategoryId` hem `CollectionId`'yi bağımsız kardeş FK olarak taşıyor — önceki oturumun PROGRESS.md notuyla birebir tutarlı.
  - **Domain:** `Product` — 28 native alan + `Category`/`Collection` navigasyonu + `Brand`/`Status` enum'ları + `CreatedAt`/`UpdatedAt` (doküman-gerekçeli istisna, Category/Collection'da yok). Çevrilebilir 6 alan (Name/ShortDescription/LongDescription/SeoUrl/MetaTitle/MetaDescription) `Translation`'da (`EntityType.Product`).
  - **Application:** `Application/Products/` (`IProductRepository`, `ProductService`, `ProductDto`, `ProductRequests`, `ProductOperationResult`, `ProductFields`, `ProductEnumDisplay`) — mevcut `ICategoryRepository`/`ICollectionRepository`/`ITranslationService`/`IUnitOfWork` değiştirilmeden tekrar kullanıldı.
  - **Infrastructure:** `ProductConfiguration` (ProductCode unique index, Category/Collection FK `Restrict`, Brand/Status `HasConversion<string>()`, decimal(10,3) alanlar), `ProductRepository` (+ `GetByProductCodeAsync`).
  - **Presentation:** `ProductController` (Index/Create/Edit/Delete — `ToggleActive` yok, Status normal Edit formundan değişiyor), Kategori(ağaç-flatten)/Koleksiyon/Marka/Durum dropdown'ları.
  - **RBAC — bilinçli kapsam sınırlaması:** Madde 30'un istediği alan-seviyeli kısmi yetkiler (İçerik Editörü: açıklama/görsel; SEO Editörü: meta alanları) **uygulanmadı** — Category/Collection ile birebir aynı action-seviyeli desen kullanıldı (Admin+Ürün Yöneticisi tam CRUD, İçerik Editörü+SEO Editörü salt görüntüleme, Admin tek başına silme). Gerekçe: projede hiç kullanılmamış yeni bir granülerlik seviyesi, yanlış uygulanırsa güvenlik riski taşır; modülün birincil kullanıcıları (Admin, Ürün Yöneticisi) günden itibaren tam çalışır durumda. Kullanıcıya raporlandı, backlog'a not düşüldü.
  - **Migration:** `AddProducts` — yalnızca `Products` tablosu; `InitialIdentity`/`AddTranslationInfrastructure`/`AddCategories`/`AddCollections`'a dokunulmadı.
  - **Doğrulama:** Build 0/0; migration şeması yalnızca beklenen `Products` tablosunu içeriyor; 20/20 iş kuralı senaryosu geçici doğrulama koduyla test edildi (geçerli oluşturma, TR ad/kısa açıklama zorunluluğu, duplicate kod reddi, var olmayan kategori/koleksiyon reddi, kalınlık≤0 reddi, zorunlu alan boşluğu reddi, upsert+opsiyonel-alan-silme, Brand/Status güncelleme, silme+Translation temizliği) — tamamı beklenen sonucu verdi; sqlcmd ile şema + tam temizlik (Products/Categories/Collections/Translations tümü 0 satır, Identity etkilenmedi) doğrulandı; anonim erişim (`/Product`, `/Product/Create`, `/Product/Delete/1`) Login'e yönlendirme ile doğrulandı. Rol-bazlı canlı test yine aynı credential kısıtı nedeniyle yapılamadı.
  - Geçici doğrulama kodu (`ProductVerification.cs` + `Program.cs`'teki `--verify-products` bloğu) doğrulama sonrası tamamen kaldırıldı.
  - Backlog madde #7 ("Ürün Yönetimi") **kısmi tamamlandı** işaretlendi — çekirdek CRUD bitti, görsel/doküman/proje ilişkileri ayrı backlog maddelerinin (#8/#9/#10) konusu.
  - **Bilinçli olarak yapılmayanlar:** Gerçek dosya yükleme/storage abstraction (ADR-006), Document/Project many-to-many ilişkileri (o entity'ler henüz yok), Excel import, arama/filtreleme/sayfalama/toplu işlem, ayrı Details ekranı, drag-drop sıralama, otomatik test projesi, alan-seviyeli RBAC (yukarıda gerekçeli).

- [x] **Task 5.1** — Ürün Görselleri Yönetimi (Backlog #8, uçtan uca) — **TAMAMLANDI (19.07.2026).**
  - **Kritik bulgu:** `Product` entity'sinde hiç `ImagePath` alanı yoktu (Task 5'te bilinçli olarak eklenmemişti) — bu, ImagePath kaldırma/koru ikilemini ortadan kaldırdı, `ProductImage` sıfırdan eklendi.
  - **Gereksinim ayrımı:** Madde 18.2'nin 5 görsel tipi (Render/Face/Lifestyle/Texture/Detail) kesin gereksinim olarak `ImageType` enum'una eklendi (task talimatındaki basitleştirilmiş çerçeveden daha zengin ama dokümana sadık); ana görsel (IsPrimary) ve 5MB/jpg-jpeg-png-webp sınırı MVP kararı; alt metin/çoklu-dil-metadata/video doküman'da yok, eklenmedi.
  - **Storage kararı (ADR-013 olarak kaydedildi):** `Application/Storage/IFileStorageService` (Stream-tabanlı, IFormFile'a bağımlı değil) + `Infrastructure/Storage/LocalFileStorageService` (wwwroot/uploads, path traversal koruması). Klasör yapısı Madde 35.4/ADR-006'ya literal sadakatle: `/uploads/products/{ProductCode}/{gorselTipi}/{guid}.{uzanti}`.
  - **Domain:** `ProductImage` (`Id`, `ProductId`/`Product`, `ImageType`, `FilePath`, `IsPrimary`, `DisplayOrder`) — `Product`'a hiçbir değişiklik yapılmadı.
  - **Application:** `Application/ProductImages/` (`IProductImageRepository`, `ProductImageService`, `ProductImageDto`, `ProductImageOperationResult`, `AddProductImageRequest`, `ProductImageEnumDisplay`) — boyut/uzantı/MIME/magic-byte doğrulama (3. parti kütüphane yok), ana-görsel otomasyonu, sahiplik kontrolü, başarısız DB yazımında fiziksel dosya geri alma. `ProductService`'e `IProductImageRepository`+`ProductImageService` enjekte edildi (liste thumbnail'i + silme kaskadı).
  - **Infrastructure:** `LocalFileStorageService`, `ProductImageConfiguration` (FK Cascade, filtered unique index `WHERE IsPrimary=1` — projenin ilk filtered unique index'i), `ProductImageRepository`.
  - **Presentation:** Ayrı `ProductImageController` (Upload/SetPrimary/MoveUp/MoveDown/Delete, Admin+Ürün Yöneticisi'ne kapalı), Product Edit'e "Görseller" bölümü, Product Index'e thumbnail sütunu.
  - **RBAC:** Madde 30'un alan-seviyeli kısmi yetki isteği (İçerik Editörü görsel düzenleyebilsin) bu task'ta da bilinçli olarak uygulanmadı — Task 5 kararı sessizce genişletilmedi.
  - **Migration:** `AddProductImages` — yalnızca `ProductImages` tablosu + filtered unique index + Cascade FK; başka hiçbir tabloya dokunmadı.
  - **Doğrulama:** Build 0/0; migration şeması yalnızca bekleneni içeriyor; 34/34 iş kuralı/güvenlik senaryosu geçti (geçerli jpg/png/webp, geçersiz uzantı/MIME/boş/boyut-aşımı/magic-byte reddi, ana görsel otomasyonu+tekilliği, sıralama, çapraz-ürün-izolasyonu, silme+fiziksel-temizlik, fallback, ürün silme kaskadı); sqlcmd ile tam temizlik doğrulandı; anonim erişim 6 endpoint'te doğrulandı; uygulama normal başladı. Rol-bazlı canlı test yine credential kısıtı nedeniyle yapılamadı.
  - Geçici doğrulama kodu (`ProductImageVerification.cs` + `Program.cs`'teki `--verify-product-images` bloğu) ve geçici `wwwroot/uploads` test dosyaları tamamen kaldırıldı.
  - `ARCHITECTURE_DECISIONS.md`'ye **ADR-013** eklendi (ADR-006/ADR-011'i tamamlar, değiştirmez) — dosya storage deseni artık Katalog/Doküman (#9), Referans Proje (#10), Blog/Banner gibi gelecek modüllerde tekrar kullanılabilir kalıcı bir ilke.
  - Backlog madde #8 ("Ürün Görselleri") **tamamen tamamlandı** işaretlendi.
  - **Bilinçli olarak yapılmayanlar:** Merkezi/generic medya kütüphanesi, Blob/S3/CDN, image-processing/otomatik crop, drag-drop (yukarı/aşağı butonlarla sıralama yeterli görüldü), toplu zip yükleme, Excel import, public site API, alan-seviyeli RBAC (yukarıda gerekçeli), video desteği, alt metin/çoklu dil metadata.

- [x] **Task 6** — Katalog / Doküman Yönetimi (Backlog #9, uçtan uca) — **TAMAMLANDI (19.07.2026).**
  - **Kritik bulgu 1 (çoklu dil, onay istenmeden çözüldü):** Madde 24 hem `DocumentName`'i multi-lang işaretliyor hem ayrı `Language` alanı listeliyor — Madde 18.3'ün dosya-başına-tek-dil isimlendirme kanıtıyla çözüldü: `Document.Title` **Translation'a taşınmadı** (native sütun), `Document.LanguageId` satırın dilini belirtiyor; TR/EN sürümleri ayrı `Document` satırlarıdır.
  - **Kritik bulgu 2 (ilişki modeli, dokümanda açıkça yazılı):** Madde 36.1/36.2, `Document`↔`Product`/`Collection`'ın **many-to-many** olduğunu (`ProductDocuments`/`CollectionDocuments` junction tablo adlarıyla) ve "genel seviye" (ilişkisiz) dokümanın mümkün olduğunu (Madde 24) açıkça belirtiyor.
  - **Kritik bulgu 3 (silme davranışı + klasör yapısı sapması, ADR-014 olarak kaydedildi):** Product/Collection silme → yalnızca ilişki satırı silinir, Document+fiziksel dosya korunur (Cascade yalnızca junction FK'lerinde). ADR-006/Madde 35.4'ün literal `/products/{urunKodu}/...` klasör örneği M2M/opsiyonel cardinality için yapısal olarak uygulanamadığından `/uploads/documents/{tipSegmenti}/{guid}.pdf` kullanıldı — ADR-006'nın ilkesi korunuyor, yalnızca product-code öneki atlandı.
  - **Yan-bulgu ve düzeltme:** `CategoryService.DeleteAsync`/`CollectionService.DeleteAsync`, Task 5'in `Product.CategoryId`/`CollectionId` Restrict FK'lerini kontrol etmiyordu — kullanımdaki kategori/koleksiyon silinmeye çalışılınca uygulama çöküyordu. `IProductRepository.HasAnyWithCategoryIdAsync`/`HasAnyWithCollectionIdAsync` eklenip her iki serviste kontrol edildi; test sırasında bizzat tetiklenip düzeltmesi doğrulandı.
  - **Domain:** `Document` (DocumentType/Title/LanguageId/FilePath/OriginalFileName/FileExtension/ContentType/FileSize/DisplayOrder/IsActive), `ProductDocument`/`CollectionDocument` (junction entity'ler).
  - **Application:** `Application/Documents/` (`IDocumentRepository`, `DocumentService`, `DocumentDto`, `DocumentRequests`, `DocumentOperationResult`, `DocumentEnumDisplay`) — PDF doğrulama (uzantı/MIME/`%PDF-` magic-byte/≤20MB), ilişki diff-upsert, ProductImage'daki telafi mantığı deseni. Mevcut `IFileStorageService` **hiç değiştirilmeden** tekrar kullanıldı.
  - **Infrastructure:** `DocumentConfiguration` (LanguageId FK Restrict), `ProductDocumentConfiguration`/`CollectionDocumentConfiguration` (Cascade + composite unique index), `DocumentRepository`.
  - **Presentation:** `DocumentController` (Index/Create/Edit/ToggleActive/Delete), Product/Collection çoklu-seçim, Dil/DocumentType dropdown'ları.
  - **RBAC — Madde 30'a literal sadakat (Task 5/5.1'den bilinçli farklı):** Admin=Tam, İçerik Editörü=yalnızca Create/Yükleme, SEO Editörü=hiç erişim yok, Ürün Yöneticisi=Tam (silme dahil — "Tam" ifadesi nedeniyle önceki modüllerin "silme yalnızca Admin" kararından sapıldı).
  - **Migration:** `AddDocuments` — yalnızca `Documents`/`ProductDocuments`/`CollectionDocuments`; başka hiçbir tabloya dokunmadı.
  - **Doğrulama:** Build 0/0; migration şeması yalnızca bekleneni içeriyor; 54/54 iş kuralı/güvenlik senaryosu geçti (geçerli PDF+ilişkiler, geçersiz uzantı/MIME/magic-byte/boş/boyut-aşımı reddi, doğrulama kuralları, genel-ilişkisiz doküman, metadata-only edit, dosya değiştirme+eski-dosya-temizliği, toggle, **Product/Collection silme → Document hayatta kalıyor** (kritik davranış testi), çapraz-izolasyon, silme DB+disk temizliği, CategoryService/CollectionService silme-guard düzeltmesi); sqlcmd ile tam temizlik doğrulandı; 6 endpoint'te anonim erişim doğrulandı; uygulama normal başladı.
  - Geçici doğrulama kodu (`DocumentVerification.cs` + `--verify-documents` bloğu) ve geçici `wwwroot/uploads` dosyaları tamamen kaldırıldı.
  - `ARCHITECTURE_DECISIONS.md`'ye **ADR-014** eklendi (ADR-006/ADR-013'ü tamamlar; klasör yapısı sapmasını gerekçeler).
  - Backlog madde #9 ("Katalog/Doküman Yönetimi") **tamamen tamamlandı** işaretlendi.
  - **Bilinçli olarak yapılmayanlar:** Otomatik thumbnail/kapak görseli üretimi (Madde 24'ün "otomatik üretilebilir" notu, PDF-render kütüphanesi gerektirir), duplicate-title kontrolü (doküman istemiyor), indirme sayacı/yayın tarihi (Madde 24'te yok), ayrı Download controller action'ı (dosyalar zaten wwwroot/uploads altında, Task 5.1'deki gibi doğrudan link ile erişiliyor), alan-seviyeli RBAC'ın genişletilmesi ötesinde ek granülerlik, otomatik test projesi.

- [x] **Task 7** — Referans Projeler Yönetimi (Backlog #10, uçtan uca) — **TAMAMLANDI (19.07.2026).**
  - **RBAC bulgusu (Madde 30 tabloda Referans Proje satırı yok, Madde 7.2 ile çözüldü):** Madde 30'un yetkilendirme tablosu Referans Proje Yönetimi'ni hiç listelemiyor. Madde 7.2 (Roller) İçerik Editörü'nün yetki kapsamını "Sayfa, blog, haber, banner, **referans proje**, ürün açıklamaları" olarak açıkça sayıyor. Bu nedenle Blog/Haber/Banner ile birebir aynı satır uygulandı: Admin=Tam, İçerik Editörü=CRUD (silme dahil — tablo bu üç modül için "CRUD" diyor, Category/Collection/Product'ın kendi bespoke "silme yalnızca Admin" kısıtlaması buraya taşınmadı), SEO Editörü=salt-görüntüleme (Category/Collection/Product'taki gibi, alan-seviyeli RBAC hâlâ hiçbir modülde uygulanmıyor), Ürün Yöneticisi=erişim yok (Madde 7.2'nin Ürün Yöneticisi tanımı "Ürün ekleme/düzenleme, Excel import, görsel yükleme, doküman ilişkilendirme" — referans projeyi kapsamıyor).
  - **Cardinality doğrulaması (Madde 23/23.1/36.1/36.2, tahmin gerekmedi):** `ReferenceProject`↔`Product` many-to-many, dokümanda "ProductProjects" adıyla anılıyor; burada `ReferenceProject` adlandırmasıyla (EntityType enum'da Task 3.1B'den beri mevcut) tutarlılık için junction `ProductReferenceProject` adı kullanıldı. Cascade FK her iki yönde — Product veya ReferenceProject silinirse yalnızca ilişki satırı gider (Document/ProductDocument ile aynı desen); Restrict FK hiçbir yönde yok, bu yüzden Task 6'daki gibi bir silme-guard yan-bulgusu bu task'ta **oluşmadı**.
  - **Görsel modeli (Madde 23.1 "Images: gallery" + "FeaturedImage: image"):** ProductImage'ın 5 tipli (Render/Face/Lifestyle/Texture/Detail) modeli **tekrar kullanılmadı** — doküman Referans Proje için tip ayrımı istemiyor, yalnızca galeri + tek kapak görseli istiyor. Bunun yerine ProductImage'ın `IsPrimary` desenindeki gibi tek `IsFeatured` bayrağıyla galeri+kapak tek tabloda (`ReferenceProjectImage`) birleştirildi — ayrı bir `FeaturedImage` sütunu/tablosu icat edilmedi (ADR-013'ün deseni, iki farklı storage mekanizması yerine). Proje kuralı gereği (`ProductImage yalnızca Product'a özeldir, başka entity kendi entity'sini oluşturacak`) `ReferenceProjectImage` sıfırdan, `ProductImage`'a dokunmadan eklendi.
  - **Klasör anahtarı sapması (ADR-014 ile aynı gerekçe):** `IFileStorageService` değiştirilmeden tekrar kullanıldı; klasör yapısı `/uploads/projects/{ReferenceProjectId}/{guid}.{uzanti}` — `ProductCode` gibi doğal bir iş kodu ReferenceProject'te olmadığı için surrogate `Id` kullanıldı (ADR-014'ün Document için yaptığı sapmanın aynısı).
  - **Zorunluluk sütunu eksikliği (doküman farkı, MVP kararı):** Madde 23.1'in Proje Veri Modeli tablosu — Product/Document'ın aksine — hiçbir "Zorunluluk" sütunu içermiyor. Müşteri Notu ("Şu an referanslara ait bir arşiv bulunmadığından ileriki fazlarda içerik eklenecektir, ancak altyapı ilk fazda hazır olmalıdır") esas alınarak yalnızca TR `ProjectName` + `ProjectType` zorunlu tutuldu; `Location`/`Architect`/`Year`/görseller/ilişkili ürünler tamamen opsiyonel — böylece "boş arşiv" ile de proje oluşturulabiliyor.
  - **Domain:** `ReferenceProject` (`Location?`, `ProjectType` enum, `Architect?`, `Year?`, `DisplayOrder`, `IsActive`), `ReferenceProjectImage` (`ReferenceProjectId`/`ReferenceProject` FK Cascade, `FilePath`, `IsFeatured`, `DisplayOrder`), `ProductReferenceProject` (junction). `Domain/Enums/ReferenceProjectType.cs` — 6 üye (Konut/Otel/Ofis/Avm/Hastane/DisMekan), Madde 23.1'in "vb." ekini içermiyor (EntityType'taki gibi spekülatif üye eklenmedi).
  - **Application:** `Application/ReferenceProjects/` (`IReferenceProjectRepository`, `ReferenceProjectService`, `ReferenceProjectDto`, `ReferenceProjectRequests`, `ReferenceProjectOperationResult`, `ReferenceProjectEnumDisplay`, `ReferenceProjectFields`) + `IReferenceProjectImageRepository`/`ReferenceProjectImageService` (ProductImageService ile birebir desen: boyut/uzantı/MIME/magic-byte doğrulama, ilk-yükleme-otomatik-kapak, silme fallback'i, DB hatasında fiziksel dosya geri alma). Mevcut `ITranslationService`/`IUnitOfWork`/`IProductRepository`/`IFileStorageService` **hiç değiştirilmeden** tekrar kullanıldı.
  - **Infrastructure:** `ReferenceProjectConfiguration`, `ReferenceProjectImageConfiguration` (filtered unique index `WHERE [IsFeatured]=1`, ProductImage'daki desenin tekrarı), `ProductReferenceProjectConfiguration` (Cascade + composite unique index), `ReferenceProjectRepository`, `ReferenceProjectImageRepository`.
  - **Presentation:** `ReferenceProjectController` (Index/Create/Edit/ToggleActive/Delete), ayrı `ReferenceProjectImageController` (Upload/SetFeatured/MoveUp/MoveDown/Delete), Ürün çoklu-seçim (`<select multiple>`), ProjectType dropdown, Edit ekranına "Görseller" bölümü (Product Edit'teki desenin aynısı).
  - **Migration:** `AddReferenceProjects` — yalnızca `ReferenceProjects`/`ReferenceProjectImages`/`ProductReferenceProjects`; başka hiçbir tabloya dokunmadı.
  - **Doğrulama (38/38 iş kuralı/güvenlik senaryosu ✅):** TR proje adı zorunluluğu, negatif sıralama reddi, var olmayan ürün ilişkisi reddi, geçerli oluşturma (Location/Architect/Year/2-ürün-ilişkisi ile), **minimal ("boş arşiv") oluşturma — yalnızca ad+tip ile** (Müşteri Notu senaryosu), upsert+opsiyonel-alan-silme (Architect boşaltıldı, ilişki 1 ürüne düşürüldü, Description silindi), toggle active, geçersiz uzantı/MIME-uyuşmazlığı/boş-dosya/boyut-aşımı/magic-byte reddi, var olmayan projeye yükleme reddi, ilk-yükleme-otomatik-kapak, ikinci görsel kapak değil, SetFeatured tekilliği, MoveUp sıralama, çapraz-proje izolasyonu (yanlış referenceProjectId ile silme reddi), silme+fallback-kapak-ataması, **ürün silme → ReferenceProject hayatta kalıyor** (kritik davranış testi, Document'takiyle aynı), proje silme → görsel DB+disk temizliği + Translation temizliği — tamamı geçici doğrulama koduyla (`ReferenceProjectService`/`ReferenceProjectImageService` doğrudan çağrılarak) test edildi, 38/38 geçti. sqlcmd ile tam temizlik doğrulandı (ReferenceProjects/ReferenceProjectImages/ProductReferenceProjects/Products/Categories/Collections/Translations tümü 0 satır, Identity etkilenmedi — 2 kullanıcı/4 rol aynı). Uygulama gerçekten başlatılıp 5 endpoint'te (`/ReferenceProject`, `/ReferenceProject/Create`, `/ReferenceProject/Edit/1`, `/ReferenceProject/Delete/1`, `/ReferenceProjectImage/Upload`) anonim erişimin Login'e yönlendirdiği ve RBAC'ın (rol yokken 302 challenge) çalıştığı curl ile doğrulandı; unhandled exception yok.
  - Geçici doğrulama kodu (`ReferenceProjectVerification.cs` + `Program.cs`'teki `--verify-reference-projects` bloğu) ve geçici `wwwroot/uploads/projects` test klasörleri tamamen kaldırıldı.
  - Yeni ADR gerekmedi — ADR-006/ADR-013/ADR-014 deseni hiçbir yeni genel-amaçlı abstraction eklenmeden üçüncü kez tekrar kullanıldı.
  - Backlog madde #10 ("Referans Proje Yönetimi") **tamamen tamamlandı** işaretlendi.
  - **Bilinçli olarak yapılmayanlar:** Alan-seviyeli RBAC, otomatik proje-ürün eşleştirme, arama/filtreleme/sayfalama/toplu işlem, ayrı Details ekranı, drag-drop sıralama, otomatik test projesi, `ReferenceProjectType`'a "vb." için genel bir "Diğer" üyesi (doküman istemediği için eklenmedi).

- [x] **Task 8** — Blog Yönetimi (Backlog #14 — kullanıcı talebinde "#11" denildi, TASKS.md'nin taslak backlog numaralandırmasında Blog Yönetimi aslında #14/Bayi-Showroom #11'dir; modül adı (Blog) açık olduğu için numaralandırma farkı durdurma nedeni sayılmadı, yalnızca burada not düşüldü) — **TAMAMLANDI (19.07.2026).**
  - **Alan sınıflandırması (Madde 21/21.1):** Title/Excerpt/Content/Slug(→SeoUrl)/MetaTitle/MetaDescription = Translation (multi-lang işaretli); Author/PublishDate/Status/FeaturedImage = native (multi-lang işaretli **değil**); Tags = native + M2M (doküman "(multi-lang)" işaretlememiş — Title/Excerpt/Content'ten bilinçli farklı); Category = ayrı `BlogCategory` entity'si (Madde 36.1 "BlogCategories" farklı bir tablo olarak listeleniyor, Product'ın Kategori Yönetimi'yle karıştırılmadı). DisplayOrder/SortOrder **eklenmedi** — Madde 21.1 tablosunda yok ve Madde 15.1 "son eklenen" ifadesi blog akışının PublishDate'e göre kronolojik olduğunu gösteriyor.
  - **BlogCategory kararı (Madde 28.2 ile netleşti):** Madde 28.2 "kategori adı"nı çoklu dil gerektiren alanlar arasında **genel olarak** (ürün/blog ayrımı yapmadan) sayıyor — bu yüzden `BlogCategory.Name` de Translation'a taşındı, yeni `EntityType.BlogCategory` eklendi (enum + `EntityTypeMapping`, migration gerektirmedi). `BlogCategory` düz/hiyerarşisiz (Category'nin 2 seviyeli ağacından farklı, doküman blog kategorileri için hiyerarşi işareti vermiyor).
  - **Cardinality:** Blog↔BlogCategory many-to-one, **nullable FK + SetNull** (Category/Collection'ın zorunlu-FK Restrict deseninden bilinçli farklı — Blog.BlogCategoryId doğası gereği opsiyonel, kategori silinirse blog etkilenmez). Blog↔Tag many-to-many (`BlogTag` junction, ProductDocument/ProductReferenceProject ile aynı desen), Tag native + paylaşılan/tekrar-kullanılabilir havuz (get-or-create, case-insensitive unique index).
  - **Görsel modeli (Madde 21.1 "FeaturedImage: image" — tekil, galeri değil):** ProductImage/ReferenceProjectImage gibi ayrı bir tablo/entity oluşturulmadı — `Blog.FeaturedImagePath` doğrudan Blog entity'sinde nullable string. Ancak yükleme/değiştirme/silme tamamen ProductImage ile aynı güvenlik desenini kullanıyor: uzantı whitelist (.jpg/.jpeg/.png/.webp), MIME çapraz kontrol, magic-byte imza doğrulama, GUID dosya adı, `IFileStorageService` (mevcut, değiştirilmeden — dördüncü modülde tekrar kullanıldı), path traversal koruması, DB hatasında fiziksel dosya geri alma, değiştirme/silme/blog-silme'de eski dosyanın temizliği.
  - **SEO kararı:** Mevcut Translation-gömülü SEO deseni (Product/Category/Collection/ReferenceProject ile birebir — `SeoUrl`/`MetaTitle`/`MetaDescription` alan sabitleri) korundu; Madde 36.1'in ayrı `SeoMeta` polimorfik tablosu **kullanılmadı** — bu, SEO veri sözleşmesi (#4) task'ının konusu, henüz yapılmadı, mevcut desenle çelişmiyor.
  - **Domain:** `Blog` (BlogCategoryId?/BlogCategory, Author?, PublishDate?, Status enum, FeaturedImagePath?), `BlogCategory` (DisplayOrder, IsActive), `Tag` (Name, unique), `BlogTag` (junction). `Domain/Enums/BlogStatus.cs` — Draft/Published/Archived (Madde 21.1'in kendi 3 durumu, gerçek enum — Category/Collection/ReferenceProject'in bool IsActive'inden bilinçli farklı, Product.Status emsali).
  - **Application:** `Application/Blogs/` — `BlogService` (Category/Collection'ın Translation-CRUD deseni + Document'ın M2M-replace deseni + ProductImage'ın tekil-görsel doğrulama deseni tek serviste birleşti), `BlogCategoryService` (Collection'ın global-duplicate-TR-ad deseniyle birebir), `ITagRepository` (get-or-create). Mevcut `ITranslationService`/`IUnitOfWork`/`IFileStorageService` **hiç değiştirilmeden** tekrar kullanıldı.
  - **Infrastructure:** `BlogConfiguration` (BlogCategoryId FK SetNull, Status HasConversion<string>), `BlogCategoryConfiguration`, `TagConfiguration` (unique index Name), `BlogTagConfiguration` (Cascade + composite unique index), `BlogRepository`, `BlogCategoryRepository`, `TagRepository`.
  - **Presentation:** `BlogController` (Index/Create/Edit/Delete — ToggleActive yok, Status enum'la Edit formundan değişiyor, Product'taki gibi), `BlogCategoryController` (Index/Create/Edit/ToggleActive/Delete — Collection'daki gibi), kapak görseli Create/Edit'e inline `IFormFile?` parametresiyle entegre (DocumentController'daki dosya-değiştirme desenine benzer, ama tek dosya/tek alan), etiketler virgülle-ayrılmış tek metin kutusundan serbest metin girişiyle.
  - **RBAC:** Madde 30'un Blog/Haber satırı **literal** uygulandı (bu kez tablo da var, ReferenceProject'teki gibi çıkarım gerekmedi): Admin=Tam, İçerik Editörü=CRUD (silme dahil), SEO Editörü=salt-görüntüleme (Madde 30'un "Meta Alanları" ifadesi projede hiç uygulanmayan alan-seviyeli RBAC yerine action-seviyeli view-only'e indirgendi — Task 5'ten beri tutarlı), Ürün Yöneticisi=erişim yok. `BlogCategoryController` aynı matrisi kullanıyor (Madde 17.2 "kategori"yi Blog Yönetimi'nin kendi fonksiyonu olarak sayıyor, ayrı RBAC satırı yok).
  - **Migration:** `AddBlog` — yalnızca `Blogs`/`BlogCategories`/`Tags`/`BlogTags`; başka hiçbir tabloya dokunmadı (`EntityType.BlogCategory` eklenmesi migration gerektirmedi, Translations.EntityType zaten esnek nvarchar).
  - **Doğrulama (35/35 iş kuralı/güvenlik senaryosu ✅):** BlogCategory TR ad zorunluluğu + global duplicate reddi, Blog TR başlık zorunluluğu, var olmayan BlogCategoryId reddi, geçerli oluşturma (kategori+yazar+tarih+durum+etiketler ile), **case-insensitive etiket tekilleştirme** ("Banyo"/"banyo" tek etikete indirgendi), minimal (yalnızca başlık) oluşturma, geçersiz uzantı/MIME-uyuşmazlığı/boyut-aşımı/magic-byte reddi, geçerli kapak yükleme, kapak değiştirme (eski dosya silinir), kapak kaldırma (dosya silinir), upsert+opsiyonel-alan-silme (Author/Excerpt boşaltıldı, BlogCategoryId null'a çekildi, etiketler değiştirildi), **kategori silme → Blog hayatta kalıyor** (SetNull davranışı doğrulandı), blog silme + Translation temizliği — tamamı geçici doğrulama koduyla (`BlogService`/`BlogCategoryService` doğrudan çağrılarak) test edildi, 35/35 geçti. sqlcmd ile tam temizlik doğrulandı (Blogs/BlogCategories/BlogTags tümü 0 satır; Tags tablosu bilinçli olarak paylaşılan havuz — 3 test etiketi manuel sqlcmd ile temizlendi, otomatik silinmiyor çünkü doküman etiket yönetimi için ayrı bir CRUD/silme akışı istemiyor). Identity etkilenmedi. Uygulama gerçekten başlatılıp 7 endpoint'te (`/Blog`, `/Blog/Create`, `/Blog/Edit/1`, `/Blog/Delete/1`, `/BlogCategory`, `/BlogCategory/Create`, `/BlogCategory/Delete/1`) anonim erişimin Login'e yönlendirdiği curl ile doğrulandı; unhandled exception yok.
  - Geçici doğrulama kodu (`BlogVerification.cs` + `Program.cs`'teki `--verify-blog` bloğu) ve geçici `wwwroot/uploads/blog` test klasörleri tamamen kaldırıldı.
  - Yeni ADR gerekmedi — ADR-006/013/014 deseni dördüncü kez tekrar kullanıldı; `EntityType.BlogCategory` eklenmesi de Task 3.1B'nin öngördüğü genişletme mekanizmasının (yeni polimorfik tip = yeni enum üyesi) ilk gerçek kullanımı, yeni bir mimari karar değil.
  - Backlog madde ("Blog Yönetimi") **tamamen tamamlandı** işaretlendi.
  - **Bilinçli olarak yapılmayanlar:** Alan-seviyeli RBAC, ayrı `TagController`/etiket yönetim ekranı (doküman istemiyor, get-or-create yeterli), WYSIWYG editör entegrasyonu (Content alanı düz `textarea`, frontend/kütüphane seçimi bu fazın kapsamı dışı), ayrı `SeoMeta` polimorfik tablosu (SEO veri sözleşmesi #4'ün konusu), arama/filtreleme/sayfalama, ayrı Details ekranı, otomatik test projesi, orphan Tag temizliği (paylaşılan havuz — bilinçli tasarım).

- [x] **Task 9** — Haber Yönetimi (Backlog #15) — **TAMAMLANDI (19.07.2026).**
  - **Alan sınıflandırması (Madde 22, prose — Blog'un 21.1 tablosu gibi ayrı bir alan tablosu yok):** Madde 22'nin tam metni: *"Haber veri modeli blog modülü ile benzer yapıda olacaktır: başlık, içerik, kapak görseli, kategori, yayın tarihi, durum ve SEO alanları."* Blog'un 21.1 tablosundaki **Excerpt, Author, Tags bu listede yok** — bilinçli olarak Haber'e kopyalanmadı (kullanıcı talimatı: "Dokümanda olmayan alan ekleme"). Title/Content = Translation; NewsCategory/PublishDate/Status/FeaturedImage = native; SEO (SeoUrl/MetaTitle/MetaDescription) = Translation. DisplayOrder yine eklenmedi (Blog ile aynı gerekçe — haber akışı da PublishDate'e göre kronolojik).
  - **NewsCategory kararı (Madde 36.1 + Madde 28.2, BlogCategory ile birebir):** Madde 36.1 "NewsCategories"i ayrı bir tablo olarak listeliyor → yeni `NewsCategory` entity (BlogCategory'nin düz/hiyerarşisiz, Translation-tabanlı Name deseninin birebir tekrarı — yeni `EntityType.NewsCategory` eklendi, migration gerektirmedi). Madde 22'nin verdiği 6 sabit kategori adı (Ödüller/Sürdürülebilirlik/Sertifikalar/Kutlamalar/Bültenler/Kurumsal) **bilinçli olarak seed edilmedi** — CategorySeeder/CollectionSeeder emsaliyle tutarlı, gerçek veri girişi Excel Import/elle-girişin konusu.
  - **Cardinality:** News↔NewsCategory many-to-one, nullable FK + SetNull (Blog↔BlogCategory ile birebir aynı). **M2M ilişki yok** — Madde 22 Haber için ne ürün ne etiket ilişkisi anıyor, bu yüzden Blog'un `BlogTag` benzeri bir junction tablosu oluşturulmadı; News, Tag/Product'tan tamamen bağımsız.
  - **Status değerleri (belirsiz, gerekçeli çıkarım):** Madde 22 Haber için kendi durum değerlerini saymıyor, yalnızca "durum" diyor. "Blog ile benzer yapı" ifadesi esas alınarak Blog.Status'un somut örneği (Taslak/Yayında/Arşiv) News için de kullanıldı — ama proje genelinde enum paylaşımı hiç yapılmadığı için (ProductStatus/BlogStatus/DocumentType hepsi ayrı) yeni, bağımsız bir `NewsStatus` enum'u oluşturuldu (BlogStatus ile aynı şekil, farklı tip).
  - **Görsel modeli (Madde 22 "kapak görseli" — tekil, galeri hiç anılmıyor):** Blog ile birebir aynı karar — ayrı bir `NewsImage` tablosu/entity'si oluşturulmadı, `News.FeaturedImagePath` doğrudan entity'de nullable string. Yükleme/değiştirme/silme güvenliği ProductImageService/BlogService ile birebir aynı (uzantı whitelist, MIME çapraz kontrol, magic-byte, GUID dosya adı, `IFileStorageService` — beşinci modülde sıfır değişiklikle tekrar kullanıldı).
  - **SEO kararı:** Blog ile birebir aynı — Translation-gömülü `SeoUrl`/`MetaTitle`/`MetaDescription` (`NewsFields`), `SeoMeta` polimorfik tablosu kullanılmadı.
  - **Domain:** `News` (NewsCategoryId?/NewsCategory, PublishDate?, Status enum, FeaturedImagePath? — Author/Excerpt/Tags yok), `NewsCategory` (DisplayOrder, IsActive). `Domain/Enums/NewsStatus.cs` — Draft/Published/Archived.
  - **Application:** `Application/News/` — `NewsService` (BlogService'in tekil-görsel-doğrulama deseninin doğrudan tekrarı, ama Tag/Product bağımlılığı yok — daha yalın), `NewsCategoryService` (BlogCategoryService ile birebir — global-duplicate-TR-ad deseni). Mevcut `ITranslationService`/`IUnitOfWork`/`IFileStorageService` **hiç değiştirilmeden** tekrar kullanıldı.
  - **Karşılaşılan derleme hatası ve çözümü:** `namespace Application.News` içinde bare `News` tipi kullanımı (`INewsRepository`, `NewsService.MapToDtoAsync` parametresi, `new News(...)` constructor çağrısı) "'News' is a namespace but is used like a type" (CS0118) hatası verdi — çünkü namespace adı (`Application.News`) ile Domain entity adı (`Domain.Entities.News`) aynı. Çözüm: bu üç noktada `Domain.Entities.News` tam nitelikli adı kullanıldı; kalıcı bir mimari değişiklik değil, yalnızca isim çakışması çözümü.
  - **Infrastructure:** `NewsConfiguration` (NewsCategoryId FK SetNull, Status HasConversion<string>, table adı literal "News"), `NewsCategoryConfiguration`, `NewsRepository`, `NewsCategoryRepository`.
  - **Presentation:** `NewsController` (Index/Create/Edit/Delete — ToggleActive yok, Status enum'la Edit formundan değişiyor), `NewsCategoryController` (Index/Create/Edit/ToggleActive/Delete — BlogCategoryController ile birebir), kapak görseli Create/Edit'e inline `IFormFile? featuredImage` parametresiyle entegre (BlogController'daki desenin aynısı, tag input'u yok).
  - **RBAC:** Madde 30'un "Blog/Haber" satırı **tek satırda birleşik** — BlogController ile birebir aynı sabitler: Admin=Tam, İçerik Editörü=CRUD (silme dahil), SEO Editörü=salt-görüntüleme, Ürün Yöneticisi=erişim yok. `NewsCategoryController` aynı matrisi kullanıyor.
  - **Migration:** `AddNews` — yalnızca `News`/`NewsCategories`; başka hiçbir tabloya dokunmadı.
  - **Doğrulama (31/31 iş kuralı/güvenlik senaryosu ✅):** NewsCategory TR ad zorunluluğu + global duplicate reddi, News TR başlık zorunluluğu, var olmayan NewsCategoryId reddi, geçerli oluşturma (kategori+tarih+durum ile), minimal (yalnızca başlık) oluşturma, geçersiz uzantı/MIME-uyuşmazlığı/boyut-aşımı/magic-byte reddi, geçerli kapak yükleme, kapak değiştirme (eski dosya silinir), kapak kaldırma (dosya silinir), upsert+opsiyonel-alan-silme (NewsCategoryId null'a çekildi, Content boşaltıldı), **kategori silme → News hayatta kalıyor** (SetNull davranışı doğrulandı), haber silme + Translation temizliği — tamamı geçici doğrulama koduyla test edildi, 31/31 geçti. sqlcmd ile tam temizlik doğrulandı (News/NewsCategories tümü 0 satır). Identity etkilenmedi. Uygulama gerçekten başlatılıp 7 endpoint'te anonim erişimin Login'e yönlendirdiği curl ile doğrulandı; unhandled exception yok.
  - Geçici doğrulama kodu (`NewsVerification.cs` + `Program.cs`'teki `--verify-news` bloğu) ve geçici `wwwroot/uploads/news` test klasörleri tamamen kaldırıldı.
  - Yeni ADR gerekmedi — ADR-006/013/014 deseni beşinci kez tekrar kullanıldı; `EntityType.NewsCategory` eklenmesi de aynı öngörülen genişletme mekanizmasının ikinci uygulaması.
  - Backlog madde #15 ("Haber Yönetimi") **tamamen tamamlandı** işaretlendi.
  - **Bilinçli olarak yapılmayanlar:** Author/Excerpt/Tags (doküman Haber için hiç anmıyor — Blog'dan körü körüne kopyalanmadı), 6 sabit kategori adının seed edilmesi (CategorySeeder/CollectionSeeder emsaliyle tutarlı), alan-seviyeli RBAC, WYSIWYG editör, ayrı `SeoMeta` tablosu, arama/filtreleme/sayfalama, ayrı Details ekranı, otomatik test projesi.

- [x] **Task 10** — Banner Yönetimi (Backlog #13) — **TAMAMLANDI (19.07.2026).**
  - **Alan sınıflandırması (Madde 16.1, prose):** *"Hero Banner yönetim panelinden yönetilebilir olacaktır: görsel/video yükleme, başlık, alt başlık, CTA butonu metni ve linki, sıralama, aktif/pasif durumu, yayın tarihi aralığı."* Title/Subtitle = Translation; ButtonText = Translation (Madde 28.2 "buton metinleri"ni açıkça çoklu-dil alanları arasında sayıyor); ButtonUrl = Translation (SeoUrl emsaliyle tutarlılık — bağlantı hedefi dile göre değişebilir); Image = native tekil `Banner.ImagePath`; DisplayOrder = native (Blog/News'ten farklı olarak **doküman burada açıkça istiyor**); IsActive = native **bool** (Blog/News'in 3-durumlu Status enum'ından bilinçli farklı — doküman burada yalnızca "aktif/pasif" 2 durum istiyor, Category/Collection/ReferenceProject deseni); PublishStartDate/PublishEndDate = native, iki alan (Blog/News'in tekil PublishDate'inden farklı — doküman açıkça "aralık" diyor).
  - **Video yükleme — bilinçli kapsam dışı bırakıldı:** Doküman "görsel/video yükleme" diyor ama mevcut dosya-doğrulama altyapısı (magic-byte imzaları, MIME whitelist) yalnızca görsel formatları (JPEG/PNG/WEBP) için kurulu; video ayrı bir format/güvenlik yüzeyi gerektirir. Bu task'ta yalnızca görsel desteklendi, video desteği ayrı bir backlog maddesi olarak bırakıldı (kritik olmadığı için durdurulmadı, raporda not düşüldü).
  - **SEO eklenmedi:** Madde 17.2/36.1 Banner için SEO hiç anmıyor (Blog/News'ten farklı) — SeoUrl/MetaTitle/MetaDescription Banner'a eklenmedi.
  - **BannerType eklenmedi:** Madde 17.2'nin "Hero banner, kampanya banner, anasayfa bileşenleri" ifadesi örnek amaçlı — DocumentType/ReferenceProjectType gibi açık bir tip taksonomisi verilmiyor, icat edilmedi. Tek düz `Banner` entity'si tüm senaryoları kapsıyor.
  - **Domain:** `Banner` (ImagePath?, PublishStartDate?, PublishEndDate?, DisplayOrder, IsActive — audit alanı yok, Category/Collection ile tutarlı). Yeni enum yok — `EntityType.Banner` zaten Task 3.1B'den beri mevcuttu, hiç kullanılmamıştı, bu task'ta ilk kez tüketildi.
  - **Application:** `Application/Banners/` — `BannerService` (Category/Collection'ın bool-IsActive+ToggleActive deseni + Blog/News'in tekil-görsel-doğrulama deseni tek serviste birleşti, artı yayın-tarihi-aralığı validasyonu [başlangıç>bitiş reddi]). Mevcut `ITranslationService`/`IUnitOfWork`/`IFileStorageService` **hiç değiştirilmeden** tekrar kullanıldı.
  - **Infrastructure:** `BannerConfiguration`, `BannerRepository`.
  - **Presentation:** `BannerController` (Index/Create/Edit/ToggleActive/Delete — Category/Collection'daki gibi, Blog/News'in aksine ToggleActive **var**), görsel Create/Edit'e inline `IFormFile? image` parametresiyle entegre (Blog/News'teki desenin aynısı).
  - **RBAC:** Madde 30'un Banner satırı literal uygulandı: Admin=Tam, İçerik Editörü=CRUD (silme dahil), SEO Editörü=— (Blog/News'ten farklı — salt-görüntüleme bile yok, SEO alanı olmadığı için tutarlı), Ürün Yöneticisi=—. Tek bir `ViewRoles`=`EditRoles` sabiti yeterli oldu (yalnızca 2 rolün herhangi bir erişimi var).
  - **Migration:** `AddBanner` — yalnızca `Banners`; başka hiçbir tabloya dokunmadı.
  - **Doğrulama (33/33 iş kuralı/güvenlik senaryosu ✅):** TR başlık zorunluluğu, negatif sıralama reddi, **yayın tarihi aralığı geçersizliği reddi** (başlangıç>bitiş), geçerli oluşturma (tarih aralığı+CTA ile), minimal (yalnızca başlık) oluşturma, ToggleActive, geçersiz uzantı/MIME-uyuşmazlığı/boyut-aşımı/magic-byte reddi, geçerli görsel yükleme, görsel değiştirme (eski dosya silinir), görsel kaldırma (dosya silinir), upsert+opsiyonel-alan-silme (tarih aralığı null'a çekildi, Subtitle/ButtonText/ButtonUrl boşaltıldı), silme + Translation temizliği — tamamı geçici doğrulama koduyla test edildi, 33/33 geçti. sqlcmd ile tam temizlik doğrulandı (Banners 0 satır). Identity etkilenmedi. Uygulama gerçekten başlatılıp 5 endpoint'te anonim erişimin Login'e yönlendirdiği curl ile doğrulandı; unhandled exception yok.
  - Geçici doğrulama kodu (`BannerVerification.cs` + `Program.cs`'teki `--verify-banner` bloğu) ve geçici `wwwroot/uploads/banners` test klasörleri tamamen kaldırıldı.
  - Yeni ADR gerekmedi — ADR-006/013/014 deseni altıncı kez tekrar kullanıldı; yeni EntityType üyesi de gerekmedi (`EntityType.Banner` zaten mevcuttu).
  - Backlog madde #13 ("Banner Yönetimi") **tamamen tamamlandı** işaretlendi.
  - **Bilinçli olarak yapılmayanlar:** Video yükleme (yukarıda gerekçeli), SEO alanları (doküman istemiyor), BannerType taksonomisi (doküman açık taksonomi vermiyor), alan-seviyeli RBAC, arama/filtreleme/sayfalama, ayrı Details ekranı, otomatik test projesi.

- [x] **Task 11** — Sayfa Yönetimi (Backlog #12) — **TAMAMLANDI (19.07.2026).**
  - **Kapsam (Madde 16.2/17.2):** "Sayfa Yönetimi \| Kurumsal sayfalar, içerik blokları, görseller, SEO alanları, dil bazlı içerik." Madde 16.2'nin 9 kurumsal sayfası (Hakkımızda, Tarihçe, NG Grup Şirketleri, Temel Değerler, Üretim, Başarılar/Ödüller, Sertifikalar, İş Birlikleri, Kariyer) kapalı bir liste değil — `Page`, Blog/News gibi serbest/açık bir içerik listesi (admin istediği kadar sayfa oluşturabilir), enum'a bağlı sabit bir sayfa tipi yok.
  - **Kritik bulgu 1 — Page'in ilk kez rastlanan "sessizliği" (IsActive/Status/PublishDate/ParentId/DisplayOrder):** Category/Collection/ReferenceProject/Banner'ın hepsinde "aktif/pasif" açıkça vardı, Blog/News'te Status enum açıkça vardı — Page için (16.2, 17.2, 30) hiçbirinden bahsedilmiyor. Literal sadakatle hiçbiri eklenmedi; kaldırma yalnızca hard-delete (Madde 30 zaten İçerik Editörü'ne "CRUD" — silme dahil — veriyor). Aynı gerekçeyle `ParentPage`/hiyerarşi ve `Menu` ilişkisi de eklenmedi (doküman ikisini de anmıyor, Madde 17.2'nin 15 modül listesinde ayrı bir Menü Yönetimi modülü yok).
  - **Kritik bulgu 2 — İçerik blok modeli (Madde 16.2, prose):** *"İçerik blokları esnek yapıda olacak: metin + görsel, tam genişlik görsel, video embed, akordeon, tab yapısı."* 5 blok tipi (`PageBlockType`: TextImage/FullWidthImage/VideoEmbed/Accordion/Tab) tek, düz bir `PageContentBlock` entity'sinde birleştirildi. **Accordion/Tab'ın çoklu panel/sekme grup yapısı dokümanda tanımlanmadığı için ayrı bir alt tablo veya grup kimliği (GroupKey/GroupId) eklenmedi — her blok bağımsız bir içerik birimidir; ardışık aynı-tipli blokların birlikte bir akordeon/sekme grubu oluşturması yalnızca bir MVP/UI yorumudur, zorunlu bir domain kuralına dönüştürülmedi.** Gelişmiş grup yönetimi gelecek faz kapsamında.
  - **Kritik bulgu 3 — Görsel modeli:** Madde 16.2 görselleri **blok** kavramının içinde tanımlıyor ("metin + görsel", "tam genişlik görsel") — `Page`'in kendi tekil/galeri görseli yok, görsel `PageContentBlock.ImagePath`'e ait (tek görsel/blok, galeri değil). Blok tipi bazlı zorunluluk: TextImage → TR içerik metni zorunlu, görsel opsiyonel ("metin + görsel" ifadesinde metin asıl unsur); FullWidthImage → görsel zorunlu; VideoEmbed → `VideoEmbedUrl` zorunlu, görsel yüklenemez (reddedilir); Accordion/Tab → TR başlık zorunlu (panel başlığı/sekme etiketi olarak kullanılabilirlik gerekçesiyle).
  - **Kritik bulgu 4 — Blok tipi değişiminde otomatik temizlik:** Blok tipi görsel-kullanmayan bir tipe değiştirildiğinde (ör. TextImage→VideoEmbed) mevcut görsel **otomatik** silinir (kullanıcı ayrıca işaretlemese bile) — hem DB hem fiziksel dosya. Blok tipi VideoEmbed'den başka bir tipe değiştirildiğinde `VideoEmbedUrl` **otomatik** temizlenir. Bu, kullanıcı talimatıyla netleşen, projenin ilk "tip değişiminde çapraz-alan temizliği" kuralı.
  - **Kritik bulgu 5 — SEO Editörü yetkisi (alan-seviyeli RBAC — teknik borç):** Madde 30 Sayfa Yönetimi için SEO Editörü'ne "Meta Alanları" düzenleme yetkisi veriyor. Blog/News/Product/Category/Collection/ReferenceProject controller'ları incelendi — **hiçbirinde alan-seviyeli RBAC altyapısı yok** (hepsi action-seviyeli, SEO Editörü her zaman yalnızca `ViewRoles`'ta, hiçbir zaman `EditRoles`'ta). Bu task'ta da aynı proje konvansiyonu korundu: SEO Editörü sayfaları görüntüleyebilir (`PageController.ViewRoles`) ama SeoUrl/MetaTitle/MetaDescription dahil hiçbir alanı düzenleyemez, silemez. **Açık teknik borç olarak kaydedildi** — "Meta Alanları" gereksiniminin tam karşılanması ayrı bir alan-seviyeli RBAC task'ının konusu (bkz. PROGRESS.md "Bilinen Riskler").
  - **Domain:** `Page` (`Id`, `CreatedAt`, `UpdatedAt` — başka native alan yok, Title/SeoUrl/MetaTitle/MetaDescription tamamen Translation'da), `PageContentBlock` (`Id`, `PageId`/`Page` FK Cascade, `BlockType` enum, `DisplayOrder`, `ImagePath?`, `VideoEmbedUrl?`, `CreatedAt`, `UpdatedAt`). `Domain/Enums/PageBlockType.cs` — 5 üye. `EntityType.Page` (Task 3.1B'den beri rezerve, ilk kez tüketildi — Banner'ın `EntityType.Banner`'ı gibi) + yeni `EntityType.PageContentBlock` eklendi (enum + `EntityTypeMapping`, migration gerektirmedi — BlogCategory/NewsCategory'nin öngörülen genişletme mekanizmasının üçüncü uygulaması). Bloğun kendi Translation namespace'ine ihtiyacı vardı çünkü `PageContentBlock.Id` ile `Page.Id` bağımsız sequence'lar — aynı `EntityType.Page` paylaşılsaydı bloklar ile sayfaların Translation kayıtları çakışırdı.
  - **Application:** `Application/Pages/` — `PageService` (Category'nin Translation-CRUD deseni, ama native alan yok), `PageContentBlockService` (ProductImage/ReferenceProjectImage'ın görsel-doğrulama+telafi deseni + Banner'ın tekil-alan deseni + blok-tipi-bazlı validasyon + tip-değişimi-temizliği — hiçbir önceki modülde olmayan yeni bir kural seti, ama sıfır yeni genel-amaçlı abstraction). Sıralama ReferenceProjectImage'daki MoveUp/MoveDown swap deseniyle (manuel sayı girişi yok). Mevcut `ITranslationService`/`IUnitOfWork`/`IFileStorageService` **hiç değiştirilmeden** tekrar kullanıldı.
  - **Infrastructure:** `PageConfiguration`, `PageContentBlockConfiguration` (BlockType `HasConversion<string>()`, PageId FK Cascade), `PageRepository`, `PageContentBlockRepository`.
  - **Presentation:** `PageController` (Index/Details/Create/Edit/Delete — Details ayrı bir ekran olarak eklendi, önceki modüllerin aksine), `PageContentBlockController` (Create/Edit/MoveUp/MoveDown/Delete — Page'in Edit ekranına gömülü, ReferenceProjectImageController'ın dosya-yükleme deseni + Banner'ın çeviri-formu deseni birleşti).
  - **RBAC:** Madde 30 satırı: Admin=Tam, İçerik Editörü=CRUD (silme dahil), SEO Editörü=Meta Alanları → proje konvansiyonuyla salt-görüntüleme'ye indirgendi (yukarıda gerekçeli), Ürün Yöneticisi=—. `PageContentBlockController` tamamen `EditRoles`'a kapalı (SEO Editörü blok içeriğine hiç erişemiyor).
  - **Migration:** `AddPages` — yalnızca `Pages`/`PageContentBlocks`; `Translations` şemasına dokunulmadı, başka hiçbir tabloya dokunulmadı.
  - **Doğrulama (45/45 iş kuralı/güvenlik senaryosu ✅):** Minimal TR-only create, TR başlık eksik reddi, çoklu dil+SEO create, upsert+opsiyonel-alan-silme (MetaDescription boşaltıldı), **5 blok tipinin her biri için oluşturma**, TextImage içerik-eksik reddi, FullWidthImage görsel-eksik reddi, VideoEmbed link-eksik reddi, **VideoEmbed+görsel kombinasyonu reddi**, Accordion/Tab başlık-eksik reddi, DisplayOrder sıralı atama, görsel değiştirme (eski dosya silinir), **blok tipi değişiminde eski görselin otomatik silinmesi**, **blok tipi değişiminde VideoEmbedUrl'in otomatik temizlenmesi**, görsel kaldırma, MoveUp sıralama, blok silme, **sayfa silme → tüm blokların+Translation'ların+fiziksel dosyaların temizlenmesi** — tamamı geçici doğrulama koduyla (`PageService`/`PageContentBlockService` doğrudan çağrılarak) test edildi, 45/45 geçti. sqlcmd ile tam temizlik doğrulandı (Pages/PageContentBlocks/Translations[PAGE,PAGE_CONTENT_BLOCK] tümü 0 satır, Identity/Languages etkilenmedi). Uygulama gerçekten başlatılıp 6 endpoint'te (`/Page`, `/Page/Create`, `/Page/Details/1`, `/Page/Edit/1`, `/PageContentBlock/Create`, `/PageContentBlock/Edit`) anonim erişimin Login'e yönlendirdiği ve POST endpoint'lerin de aynı şekilde yetkisiz reddedildiği curl ile doğrulandı; unhandled exception yok.
  - Geçici doğrulama kodu (`PageVerification.cs` + `Program.cs`'teki `--verify-pages` bloğu) tamamen kaldırıldı; test sırasında oluşan `wwwroot/uploads/pages` dosyaları servis katmanının kendi temizlik mantığıyla otomatik silindi (manuel ek temizlik gerekmedi).
  - Yeni ADR gerekmedi — ADR-006/013/014 deseni yedinci kez tekrar kullanıldı; `EntityType.PageContentBlock` eklenmesi ADR-012'nin öngördüğü genişletme mekanizmasının üçüncü uygulaması (BlogCategory/NewsCategory'den sonra), yeni bir mimari karar değil.
  - Backlog madde #12 ("Sayfa Yönetimi") **tamamen tamamlandı** işaretlendi.
  - **Bilinçli olarak yapılmayanlar / açık teknik borçlar:** Accordion/Tab çoklu panel/sekme grup yönetimi (ayrı alt tablo/grup kimliği — dokümanda tanımlanmadığı için gelecek faz), SEO Editörü'nün SeoUrl/MetaTitle/MetaDescription'ı düzenleyebilmesi (alan-seviyeli RBAC altyapısı projede hiç yok — ayrı bir RBAC task'ının konusu), ParentPage/hiyerarşi, Menu ilişkisi, IsActive/Status/PublishDate/DisplayOrder (doküman hiçbirini istemiyor), drag-drop sıralama (yukarı/aşağı yeterli görüldü), otomatik test projesi.

- [x] **Task 12** — Testing Foundation, Test Altyapısı, Seeder Sertleştirme, Docker ve Migration/Index Audit — **TAMAMLANDI (19.07.2026).**
  - **Bu task yeni bir CMS modülü değil** — 11 içerik modülünün ardından projenin ilk kalıcı otomatik test altyapısı, seeder güvenilirliği, konteynerleştirme ve şema denetimi task'ı. Talimat 21 madde + "durulması gereken durumlar" + "kısıtlar" listesiyle geldi; hiçbir kısıt ihlal edilmedi.
  - **Kritik bulgu 1 — Page↔PageContentBlock ilişki doğrulaması (düzeltme değil):** Task 11 kapanış raporundaki "1:1-sahipli" ifadesi belirsiz bulundu, kod incelendi: `PageContentBlockConfiguration.cs`'te `HasOne(b => b.Page).WithMany().HasForeignKey(b => b.PageId)` — gerçek ilişki **bire-çok**, kod/migration hiçbir zaman yanlış değildi, yalnızca dokümantasyon dili yanlıştı. PROGRESS.md'deki 3 ifade "bire-çok"/"tek-sahipli" olarak düzeltildi, configuration yorumu netleştirildi. `RelationalConstraintTests.cs`'te bu davranış (bir Page'in birden fazla bloğu olabilmesi + Cascade delete) integration test ile doğrulandı.
  - **Kritik bulgu 2 — Unit/Integration ayrımı ve test stratejisi:** `UseInMemoryDatabase` **kullanılmadı** (FK/unique/cascade davranışını uygulamıyor, yanlış-pozitif riski). Bunun yerine **SQLite in-memory** + gerçek repository/UnitOfWork kullanan, yalnızca dosya I/O ve Translation persistence'ı sahteleyen "sociable unit test" deseni benimsendi — `BlogService`/`PageService`/`ProductService` gibi servislerin EF-atanan (`private set`) Id'lere bağımlı two-phase create akışı saf Moq ile pratik test edilemediği için.
  - **Kritik bulgu 3 — SQLite/SQL Server uyumsuzlukları:** `Translation.Value`'nun `nvarchar(max)` raw column type'ı ve üretim veritabanının gerçek case-insensitive collation'ı (`SQL_Latin1_General_CP1_CI_AS`, sqlcmd ile doğrulandı) SQLite'ta doğrudan çalışmıyordu. Üretim configuration/migration dosyalarına dokunulmadan, yalnızca test projesinde devreye giren `SqliteCompatibleModelCustomizer` (`IModelCustomizer`) ile adapte edildi.
  - **Kritik bulgu 4 — Seeder sertleştirme + startup politikası:** `IdentitySeeder`/`LanguageSeeder` zaten idempotent'ti ama hiç testi yoktu — `IdentitySeederTests`/`LanguageSeederTests` eklendi (ilk/ikinci çalıştırma, duplicate yok, parola/rol overwrite yok, eksik config'te güvenli atlama). `DatabaseInitialization:ApplyMigrationsOnStartup`/`SeedOnStartup` (varsayılan `false`/`true`) config bayrakları eklendi — ADR-004'ün açık bıraktığı "production'da migration nasıl tetiklenecek" sorusunu config-driven biçimde kapatıyor, ADR-004'ü geçersiz kılmıyor (yeni ADR gerekmedi).
  - **Kritik bulgu 5 — Docker altyapısı sıfırdan oluşturuldu, canlı doğrulama yapılamadı:** Multi-stage `Dockerfile`, `docker-compose.yml` (uygulama+SQL Server+healthcheck+`depends_on: condition: service_healthy`+kalıcı volume), `.env.example` (gerçek `.env` commit edilmedi, `.gitignore`'a eklendi) oluşturuldu. **Docker Desktop bu makinede bu oturum boyunca çalışır duruma getirilemedi** (`npipe` bağlantı hatası, birden fazla başlatma denemesi) — dürüstçe açık bir ortam kısıtı olarak raporlandı. Yerine, Docker'ın izleyeceği aynı kod yolu (config-driven migrate+seed) gerçek SQL Server'a karşı boş bir veritabanıyla iki kez çalıştırılarak (fresh migration + idempotent 2. çalıştırma, satır sayısı+checksum karşılaştırmalı) doğrulandı; geçici veritabanları (`_FreshTest`, `_DockerStyleTest`, `_FinalCheck`) test sonrası temizlendi, gerçek geliştirme veritabanına hiç dokunulmadı.
  - **Kritik bulgu 6 — Migration/Index audit:** Tüm 12 migration dosyası tek tek okunarak gözden geçirildi, hiçbiri geriye dönük değiştirilmedi. Tüm configuration + repository sorgu desenleri taranarak yalnızca gerçek sorgu kanıtı olan 2 eksik composite index bulundu (`PageContentBlocks`, `ProductImages` — ikisi de `WHERE FK=@p ORDER BY DisplayOrder` çalıştırıyordu) — tek, açık isimli corrective migration (`AddPerformanceAndConstraintIndexes`) ile düzeltildi. Spekülatif indexler (Product.Status/Brand/CreatedAt, Blog/News Status+PublishDate) kod tabanında karşılık gelen sorgu olmadığı için **eklenmedi** — bilinçli "şimdi değil" kararı.
  - **Test projeleri:** `tests/NGKutahyaSeramik.UnitTests` (51 test) + `tests/NGKutahyaSeramik.IntegrationTests` (37 test) — xUnit + FluentAssertions (6.12.1, lisans nedeniyle pinlendi) + Moq + `Microsoft.AspNetCore.Mvc.Testing` + `Microsoft.EntityFrameworkCore.Sqlite` + coverlet.collector. Ortak altyapı: `SqliteTestDatabase`, `SqliteCompatibleModelCustomizer`, `ServiceTestContext`, `IdentityTestHost`, `FakeUnitOfWork`, 9 model factory, `FakeTranslationService`/`FakeFileStorageService`, `CustomWebApplicationFactory`, `TestAuthHandler` (`X-Test-Role` header, AntiForgery'ye dokunmuyor), `AntiForgeryHelper` (gerçek GET+token+POST akışı).
  - **Doğrulama:** Clean build (`dotnet clean`→`restore`→`build`) 0 Warning/0 Error; `dotnet test` → **88/88 geçti** (51 unit + 37 integration); coverage: global %11.5 line/%18 branch/%32.9 method (bilinçli düşük — yalnızca Blog/Page/PageContentBlock/Product+2 seeder hedeflendi), hedeflenen sınıflarda `BlogService` %76.3, `PageService` %93.3, `PageContentBlockService` %75.5, `ProductService` %74.9, `IdentitySeeder` %80.8, `LanguageSeeder` %100; fresh-database migration testi (boş DB→29 tablo, 2 kullanıcı/4 rol/7 dil seed) + idempotent 2. çalıştırma (satır sayısı+checksum birebir aynı) sqlcmd ile doğrulandı, geçici DB temizlendi.
  - `TESTING.md` sıfırdan oluşturuldu (test türleri, factory/mock kullanımı, coverage komutu, AntiForgery/RBAC test yaklaşımı, migration/index audit standardı, dış API mock standardı, gelecek modüller için Definition of Done checklist'i).
  - Yeni ADR gerekmedi. Backlog'a yeni madde eklenmedi (altyapı task'ı, içerik modülü değil) — madde 21'in ("Test senaryolarının panel/API tarafına uygulanması") temeli bu task ile atıldı, ama yalnızca 4 modül hedeflendiği için tamamen kapatılmadı.
  - **Bilinçli olarak yapılmayanlar:** Category/Collection/Document/ReferenceProject/News/Banner servislerinin unit testleri (foundation kapsamı dışı, gelecek task), gerçek Docker canlı doğrulaması (ortam kısıtı), gerçek dış API mock sistemi (proje henüz hiçbir dış API'ye bağlı değil, yalnızca standart TESTING.md'de belgelendi), coverage eşiği zorlaması (anlamsız test yazımını önlemek için bilinçli kaçınıldı).

- [x] **Task 13** — Haber Yönetimi (News) Test Sertleştirmesi — **TAMAMLANDI (19.07.2026).**
  - **Kritik bulgu (task numarasıyla gerçek durum çelişkisi, kullanıcıya soruldu):** Prompt "Haber Yönetimi'ni uçtan uca geliştir" diye geldi ve modülü sıfırdan kuruluyormuş gibi tarif ediyordu (yeni Domain, yeni migration `AddNews`, yeni controller'lar). Ama PROJECT_MEMORY.md/TASKS.md okunduğunda **News/NewsCategory'nin zaten Task 9'da (19.07.2026) tam kurulmuş** olduğu görüldü — Domain/Application/Infrastructure/Presentation/migration hepsi mevcut, 31/31 senaryo doğrulanmıştı; Task 12 ise News'i bilinçli olarak unit test kapsamı dışında bırakmıştı (TASKS.md madde 21'de açıkça not düşülü). Bu durum, prompt'un kendi "durman gereken durumlar" listesindeki *"Mevcut Blog/News tabloları arasında veri kaybı riski taşıyan yeniden kullanım kararı gerekiyorsa"* maddesiyle birebir örtüştüğü için **AskUserQuestion ile durulup onay istendi**. Kullanıcı "Mevcut News modülünü test standardına yükselt" seçeneğini onayladı — Domain/Application/Infrastructure/Presentation'a dokunmadan, Task 12'nin Blog/Page/PageContentBlock/Product için kurduğu kalıcı test standardını News/NewsCategory'ye uygulamak.
  - **Yapılmayan (bilinçli, çünkü zaten mevcut):** Yeni Domain entity/enum, yeni Application servisi, yeni Infrastructure configuration/repository, yeni Controller/View, yeni migration — hiçbiri oluşturulmadı; mevcut News/NewsCategory kodu **hiç değiştirilmedi**.
  - **Test Altyapısı:** `tests/NGKutahyaSeramik.UnitTests/Factories/NewsFactory.cs` (+ `NewsCategoryFactory`) — `BlogFactory`/`BlogCategoryFactory` deseninin birebir tekrarı, yeni test projesi oluşturulmadı.
  - **Unit testler (24 yeni):** `NewsServiceTests` (16) — TR başlık zorunluluğu, tüm translation alanlarının kaydı, opsiyonel alan temizliği, var olmayan kategori reddi, geçerli kategori kabulü, kategorisiz kabul, kategori silme→SetNull (News hayatta kalıyor), 3 Status değeri, PublishDate nullable, kapak görseli yükleme/değiştirme(eski dosya silme)/kaldırma, silme→Translation+fiziksel dosya temizliği. `NewsCategoryServiceTests` (8) — TR ad zorunluluğu, negatif sıralama reddi, case-insensitive duplicate reddi, aynı-ad-ile-güncelleme, toggle active, silme→Translation temizliği, var olmayan kategori silme reddi.
  - **Integration testler (19 yeni):** `AnonymousAccessTests` (+7: News/NewsCategory GET+POST endpoint'leri), `RbacTests` (+8: ViewRoles/EditRoles/ProductManager-reddi/SeoEditor-reddi matrisi, Blog'daki ile birebir aynı — Admin+İçerik Editörü+SEO Editörü görüntüleme, Admin+İçerik Editörü düzenleme, Ürün Yöneticisi erişim yok), `AntiForgeryAndPrgTests` (+3: geçerli token+PRG→302, token'sız→400, geçersiz ModelState→200), `RelationalConstraintTests` (+1: `DeletingNewsCategory_SetsNewsCategoryIdToNull_NewsSurvives`, SQLite üzerinde gerçek FK davranışı).
  - **Index/SEO review sonucu — yeni migration gerekmedi:** `NewsRepository.GetAllAsync()`/`NewsCategoryRepository.GetAllAsync()` incelendi — hiçbir SQL-seviyesi `.Where`/`.OrderBy` yok (`NewsController.Index()`'teki `OrderByDescending(PublishDate)` zaten materialize edilmiş DTO listesi üzerinde bellek-içi sıralama, SQL değil). Bu, Task 12'nin index audit'inde "Blog/News Status+PublishDate — kod tabanında karşılık gelen sorgu yok, eklenmedi" kararıyla tam tutarlı; karar yeniden doğrulandı, yeni index/migration **eklenmedi**. SEO URL tekilleştirmesi sistem genelinde hâlâ yok (diğer tüm modüllerle tutarlı, yeni bir teknik borç değil, mevcut olanın teyidi).
  - **Doğrulama:** `dotnet clean`→`restore`→`build` 0 Warning/0 Error; solution toplamı **131/131 test geçti** (75 unit [51+24 yeni] + 56 integration [37+19 yeni]), 0 başarısız, 0 atlanmış. Coverage: global %14.1 line/%24.2 branch/%40.7 method (Task 12'nin %11.5'inden yükseldi); `NewsService` %80.5 line, `NewsCategoryService` %88.6 line. Migration/database update gerekmedi (şema değişmedi). Docker'a **hiç dokunulmadı** (talimatla açıkça yasaklandı, final stabilization task'ına bırakıldı). Geçici doğrulama kodu/artefakt bırakılmadı.
  - Backlog madde 21'in ("Test senaryolarının panel/API tarafına uygulanması") kapsamı News eklenerek genişledi — Blog/Page/PageContentBlock/Product/News artık test kapsamında, Category/Collection/Document/ReferenceProject/Banner hâlâ bekliyor.
  - Yeni ADR gerekmedi — mevcut mimari kararların hiçbiri değişmedi, yalnızca test kapsamı genişledi.

- [x] **Task 14** — Bayi / Showroom Yönetimi (Backlog #11) — **TAMAMLANDI (19.07.2026).**
  - **Gerçekten yeni bir modül** (Task 13'ün aksine) — backlog #11 daha önce hiç başlanmamıştı. ADR-008 (Task 0.2) zaten "Tek `Dealer` entity + `Category` alanı" ilkesini onaylamıştı ama 3 nokta bilinçli açık bırakılmıştı: bu task, Madde 25/25.1/25.2/26/30/36.1/Ek-2'yi inceleyerek bunları kesinleştirdi.
  - **Kritik bulgu 1 — 212/187/8/17 sayıları doğrulandı:** Madde 25 ve Ek-2 birebir aynı sayıları veriyor (212 toplam, 187 bayi/kategori-2, 8 showroom/kategori-3, 17 kategorisiz, 63 farklı şehir) — dokümanın kendi içinde tutarlı, gerçek kaynak veri. **212 kayıt bu task'ta seed edilmedi** (talimatla yasaklandı) — yalnızca şema/enum kuruldu.
  - **Kritik bulgu 2 — Translation kullanılmıyor:** Madde 25.1'in veri modeli tablosu (`DealerId, Name, City, District, Address, Phone, Fax, Email, Latitude, Longitude, Category, Region, RegionName, Status`) — Product/Blog/Proje tablolarının aksine — hiçbir alanı "(multi-lang)" işaretlemiyor. `Dealer`, **projenin Translation altyapısını hiç tüketmeyen ilk CMS modülü**; `EntityType.Dealer` (Task 3.1B'den beri rezerve) hâlâ tüketilmedi. Aynı gerekçeyle SEO alanı da eklenmedi — Madde 27.2'nin Bayi URL deseni (`/satis-noktalari/{il}/{ilce}`) City+District'ten programatik türetiliyor, per-kayıt özel SeoUrl/MetaTitle/MetaDescription gerektirmiyor.
  - **Kritik bulgu 3 — Görsel/galeri/açıklama/çalışma saatleri/randevu formu/sıralama eklenmedi:** Madde 25.1'in gerçek veri modeli tablosunda bu alanların hiçbiri yok; yalnızca Madde 26'nın "showroom sayfaları... galeri görselleri, açıklama, çalışma saatleri... yer alacaktır" ifadesinde geçiyorlar — ama aynı paragraf modül ayrımını da "Karar Bekleniyor" bırakıyor ve ADR-008 bunu zaten "otomatik zorunlu değil" işaretlemişti. Bu, dokümanın **public site anlatımı** (ADR-001/002/009 gereği bu fazın kapsamı dışı), admin CRUD veri modelini bağlamıyor. `IFileStorageService` bu modülde **hiç kullanılmadı** — Task 5.1'den beri kurulu storage altyapısını atlayan ilk CMS modülü.
  - **Kritik bulgu 4 — Kategorisiz (17) kayıt kararı:** `Dealer.Category` **nullable** (`DealerCategory?`) yapıldı — yeni bir "Unclassified" enum üyesi icat edilmedi, mevcut nullable-FK deseniyle (BlogCategoryId?/NewsCategoryId?) tutarlı. Enum'un sayısal değerleri dokümanla birebir: `Dealer = 2, Showroom = 3` (0/1 değil) — gelecekteki Excel Import'un legacy kod eşlemesiyle uyumlu.
  - **Kritik bulgu 5 — RBAC, projedeki ilk salt-Admin modül:** Madde 30 Bayi/Showroom satırı: "Tam | — | — | —" — Admin=Tam, diğer 3 rolün (İçerik Editörü/SEO Editörü/Ürün Yöneticisi) **hiçbir erişimi yok** (salt-görüntüleme dahil). Diğer tüm modüllerde en az bir rol salt-görüntüleme yetkisine sahipti. `DealerController`'da tek `[Authorize(Roles = ApplicationRoles.Admin)]` yeterli oldu.
  - **Domain:** `Dealer` (Id, Name/City zorunlu, Category nullable enum, District/Address/Phone/Fax/Email/Region/RegionName opsiyonel, Latitude/Longitude nullable `decimal(9,6)`, IsActive bool + Activate/Deactivate). `Domain/Enums/DealerCategory.cs` — `Dealer=2, Showroom=3`.
  - **Application:** `Application/Dealers/` — `DealerService` (Translation ve IFileStorageService **hiç kullanmıyor**, projedeki en yalın servis; Name/City zorunluluğu, Latitude -90..90/Longitude -180..180 aralık doğrulama, "ikisi birlikte veya hiçbiri" koordinat kuralı, ToggleActive). Mevcut `IUnitOfWork` değiştirilmeden tekrar kullanıldı.
  - **Infrastructure:** `DealerConfiguration` (Category `HasConversion<string>()`, Latitude/Longitude `decimal(9,6)` — float/double kullanılmadı), `DealerRepository`.
  - **Presentation:** `DealerController` (Index/Create/Edit/Delete/ToggleActive, Admin-only), Index'te Category+City filtreleme (in-memory, SQL'e itilmiyor — projenin genel listeleme desenine tutarlı).
  - **Index review:** `DealerRepository`'de hiçbir SQL-seviyesi `.Where`/`.OrderBy` yok — yeni index eklenmedi, mevcut PK yeterli.
  - **Migration:** `AddDealers` — yalnızca `Dealers` tablosu; başka hiçbir tabloya dokunmadı. Mevcut geliştirme veritabanına uygulandı.
  - **Doğrulama:** Clean build 0 Warning/0 Error; solution toplamı **165/165 test geçti** (92 unit [75+17 yeni] + 73 integration [56+17 yeni]). Coverage: global %14.3 line; `DealerService` %93.4 line — projedeki en yüksek servis coverage'ı. Docker'a hiç dokunulmadı (talimatla yasaklandı).
  - `ARCHITECTURE_DECISIONS.md`'deki **ADR-008 güncellendi** (yeni ADR değil) — 3 açık nokta bu task'ta kesinleşti, ADR artık tamamen kapalı.
  - Backlog madde #11 ("Bayi/Showroom") **tamamen tamamlandı** işaretlendi.
  - **Bilinçli olarak yapılmayanlar:** 212 gerçek kayıt seed etme (Excel Import #17'nin konusu), harita entegrasyonu (Google Maps API vb. — public site'ın konusu), galeri/açıklama/çalışma saatleri/randevu formu (Madde 25.1'in kesin tablosunda yok), SEO alanları (Madde 25.1'de yok), Excel Import/SAP/CRM entegrasyonu (talimatla açıkça yasaklandı, ayrı backlog/Faz 2 maddeleri), otomatik test projesi (mevcut altyapı kullanıldı).

- [x] **Task 15** — Form Yönetimi (Backlog #16) — **TAMAMLANDI (20.07.2026).**
  - **Gerçekten yeni bir modül** (Task 14 gibi). Madde 17.2 ("Form Yönetimi | İletişim ve bilgi talep formları listesi, durum takibi, e-posta bildirimleri") + Madde 29 (Formlar) incelendi.
  - **Kritik bulgu 1 — Modül kapsamı, dinamik form builder DEĞİL:** Madde 29.1/29.2/29.3 yalnızca 3 form türünü somut alan listesiyle tanımlıyor: İletişim, Request Information/Bilgi Talep, Numune Talep. Madde 36.1'in "Forms | Form gönderileri | FormFields" ifadesi ve Madde 17.2'nin "Form Yönetimi" satırı yalnızca **gelen form kayıtlarının yönetimini** tarif ediyor — admin'in yeni form tanımlayabildiği bir builder ekranı hiçbir yerde yok. `FormDefinition`/`FormField`/drag-and-drop builder/JSON blob **kurulmadı**.
  - **Kritik bulgu 2 — Domain kararı (Seçenek 1):** Tek `FormSubmission` entity + `FormType` enum discriminator (Contact/RequestInformation/SampleRequest) + tip-özel nullable sütunlar (Subject; ProductCode/ProductName; Address/RequestedProduct/Quantity). Ayrı detay entity'leri veya JSON blob, 3 tür + birkaç skaler tip-özel alan için haksız karmaşıklık olurdu; admin panelinin "tek gelen kutusu" ihtiyacı da (Madde 17.2) tek tabloyu destekliyor.
  - **Kritik bulgu 3 — Durum: Status enum yerine IsRead/ReadAt/ProcessedAt:** Madde 17.2 yalnızca soyut "durum takibi" diyor, Blog/News/Product'taki gibi somut değer listesi vermiyor. İcat edilmiş bir enum yerine nullable zaman damgaları kullanıldı (`ProcessedAt` dolu = işleme alındı) — Banner'ın PublishStartDate/EndDate deseniyle tutarlı.
  - **Kritik bulgu 4 — Translation ve dosya eki hiç kullanılmadı:** Tüm alanlar kullanıcı tarafından girilen ham veri — Dealer'dan sonra Translation'ı hiç tüketmeyen ikinci modül. Madde 29.1/29.2/29.3'ün hiçbirinde dosya/CV eki yok — `IFileStorageService` kullanılmadı.
  - **Kritik bulgu 5 — Public form gönderim endpoint'i ve e-posta bildirimi bu fazda kurulmadı:** ADR-001/002/009 gereği public site kodu bu fazda hiç yazılmıyor. `FormSubmissionService.CreateSubmissionAsync` Application katmanında hazır ve unit testlerle doğrulanmış (gelecekteki public site doğrudan çağırabilir) ama hiçbir `[AllowAnonymous]` controller'dan çağrılmıyor. E-posta bildirimi de bu var-olmayan public akışın sonucu olduğu için kurulmadı (SMTP/MailKit altyapısı eklenmedi, kritik belirsizlik olmadığı için onay istenmedi — doğrudan ADR-001/002/009'un zaten onaylı sınırından çıkıyor).
  - **Kritik bulgu 6 (asıl yeni mimari desen, ADR-015) — Projedeki ilk gerçek SQL-seviyesi pagination/filtreleme:** Form kayıtları zamanla sürekli büyüyen bir veri seti — `IFormSubmissionRepository.GetPagedAsync(FormSubmissionQuery)` gerçek `IQueryable.Where/OrderByDescending/Skip/Take` + ayrı `CountAsync()` kullanıyor, hiçbir noktada tüm tablo belleğe çekilmiyor. Önceki tüm modüllerin (Category'den News'e) `GetAllAsync()`+in-memory-filtreleme deseninden **bilinçli sapma** — TESTING.md §7A'ya ve ADR-015'e gelecekteki modüller için "ne zaman pagination, ne zaman in-memory listeleme" ilkesi olarak kaydedildi.
  - **Domain:** `FormSubmission` (Id, FormType, FullName/Email/Phone zorunlu, Company opsiyonel, Message zorunlu, ConsentAccepted; Subject/ProductCode+ProductName/Address+RequestedProduct+Quantity tip-özel nullable; IsRead+ReadAt, ProcessedAt, AdminNote, CreatedAt). `Domain/Enums/FormType.cs`.
  - **Application:** `Application/Forms/` — `FormSubmissionService` (Translation/IFileStorageService kullanmıyor; FormType'a göre koşullu zorunlu alan doğrulaması; e-posta `GeneratedRegex` ile — AntiForgeryHelper'daki desenle tutarlı), `FormSubmissionQuery`/`PagedResult<T>`.
  - **Infrastructure:** `FormSubmissionConfiguration` (2 index: `CreatedAt` — her Index sayfası bununla sıralanıyor; `FormType+CreatedAt` — tip filtresi gerçek sorgu deseni. IsRead/Email/Phone'a index eklenmedi — düşük kardinalite/LIKE-arama sınırlı fayda), `FormSubmissionRepository` (gerçek `IQueryable` filtreleme+sayfalama).
  - **Presentation:** `FormSubmissionController` (Index [filtre+pagination]/Details/MarkAsRead/MarkAsUnread/MarkAsProcessed/MarkAsUnprocessed/UpdateAdminNote/Delete). Details GET'inde **bilinçli olarak** otomatik okundu-işaretleme yapılmadı (GET idempotent kalmalı + İçerik Editörü'nün "Görüntüleme" yetkisiyle dolaylı yazma tetiklememesi için). Liste ekranında Message/AdminNote gösterilmiyor (yalnızca Details'te) — kişisel veri asgari düzeyde. Razor otomatik HTML-encode ile XSS'e karşı korunuyor, ayrı sanitize kütüphanesi eklenmedi.
  - **RBAC:** Madde 30 Form Yönetimi satırı literal: Admin=Tam, İçerik Editörü=Görüntüleme, SEO Editörü=—, Ürün Yöneticisi=—. ViewRoles=Admin+İçerikEditörü, EditRoles=yalnızca Admin.
  - **Migration:** `AddFormSubmissions` — yalnızca `FormSubmissions` tablosu + 2 index; başka hiçbir tabloya dokunmadı. Mevcut geliştirme veritabanına uygulandı.
  - **Doğrulama:** Clean build 0 Warning/0 Error; solution toplamı **202/202 test geçti** (116 unit [92+24 yeni] + 86 integration [73+13 yeni]). Coverage: global %15.1 line; `FormSubmissionService` %85.2, `FormSubmissionRepository` %92.6 (pagination/filtre kodu iyi test edilmiş) — projedeki en yüksek repository coverage'ı. Docker'a hiç dokunulmadı (talimatla yasaklandı).
  - `ARCHITECTURE_DECISIONS.md`'ye **ADR-015** eklendi (yeni ADR) — tek tablo+discriminator kararı + projedeki ilk gerçek pagination deseninin gelecekteki modüller için ilke olarak kaydı. `TESTING.md`'ye §7A ("Büyüyen Veri Setleri İçin Pagination/Sorgu Testleri") eklendi — yeni bir test örüntüsü oluştuğu için.
  - Backlog madde #16 ("Form Yönetimi") **tamamen tamamlandı** işaretlendi — yalnızca admin veri modeli/servis/UI kapsamında; public gönderim endpoint'i ve e-posta bildirimi açık teknik borç/gelecek faz maddesi olarak kaydedildi.
  - **Bilinçli olarak yapılmayanlar:** Dinamik form builder (doküman istemiyor), public `[AllowAnonymous]` form gönderim endpoint'i (ADR-001/002/009 sınırı), e-posta bildirimi/SMTP altyapısı (var olmayan public akışın sonucu), dosya eki/CV yükleme (doküman istemiyor), CAPTCHA/reCAPTCHA (public form olmadığı için tetiklenmiyor, Madde 29.4'ün gerektirdiği yer public gönderim noktası), Excel export (doküman istemiyor, Dealer Excel Import ile karıştırılmadı), gerçek form verisi seed etme, Status enum (doküman somut değer vermiyor), otomatik test projesi (mevcut altyapı kullanıldı).

- [x] **Task 16** — Kullanıcı Yönetimi: Analiz (Backlog #2) — **TAMAMLANDI (20.07.2026), kod değişikliği yok.**
  - Talimat gereği yalnızca analiz — dosya oluşturma/değiştirme/migration/paket ekleme yasaktı. PROJECT_MEMORY.md/PROGRESS.md/TASKS.md/ARCHITECTURE_DECISIONS.md/TESTING.md + referans dokümanın Madde 7.2/17.2/30/31 bölümleri + mevcut `ApplicationUser`/`ApplicationRoles`/`IdentitySeeder`/`AccountController`/`AppDbContext`/test altyapısı (`IdentityTestHost`/`CustomWebApplicationFactory`/`TestAuthHandler`) incelendi.
  - 30 başlıklı bir analiz raporu üretildi: dokümanda Kullanıcı Yönetimi için yalnızca "Tam=Admin, diğer 3 rol=—" (Madde 30) ve "panel kullanıcıları, rol tanımlama, yetki atama" (Madde 17.2) var — somut bir veri modeli tablosu yok (Product/Blog'un aksine), bu yüzden alan seti Identity'nin kendi zorunlu alanlarından + RBAC ihtiyacından çıkarıldı.
  - **Role Management (dinamik rol tanımlama/silme) kullanıcı talimatıyla açıkça kapsam dışı bırakıldı** — yalnızca mevcut 4 sabit role kullanıcı ataması bu görevin konusu.
  - 3 kritik belirsizlik (`IsActive` vs Identity Lockout; parola belirleme yöntemi — proje genelinde SMTP/e-posta altyapısı olmadığı Task 15'te zaten doğrulanmıştı; silme davranışı — hard-delete vs yalnızca pasifleştirme) `AskUserQuestion` ile kullanıcıya soruldu, üçü de önerilen seçenekle onaylandı.
  - Kendi-hesabını-kilitleme/silme ve son-Admin-kaldırma riskleri (dokümanda yazılı değil, mantıksal zorunluluk) kesin gereksinim olarak işaretlendi.

- [x] **Task 16B** — Kullanıcı Yönetimi: İmplementasyon (Backlog #2) — **TAMAMLANDI (20.07.2026), implementasyon başlamadan hemen önce 2 karar revize edildi.**
  - Task 16'da kesinleşen 3 kararla uçtan uca implementasyon: `ApplicationUser.IsActive` (bool, varsayılan true), admin'in formda doğrudan parola belirlemesi, hard-delete + guardrail'ler.
  - **Revizyon (kullanıcı talimatı, implementasyon kodu yazılmadan önce):**
    1. **Çoklu rol → tek rol:** Analiz raporunda önerilen çoklu-rol desteği (Identity'nin doğal many-to-many yapısı) kullanıcı tarafından kaldırıldı — referans dokümandaki roller birbirini dışlayan iş rolleri olduğu ve MVP'de tek rolün yeterli olduğu gerekçesiyle. `CreateUserRequest.Role`/`UpdateUserRequest.Role`/`UserDto.Role` artık tekil `string` (eski `Roles: IReadOnlyList<string>` değil). `CreateAsync` tam olarak bir rolü `AddToRoleAsync` ile atıyor; `UpdateAsync` rol değiştiğinde eski rolü `RemoveFromRoleAsync` ile kaldırıp yenisini `AddToRoleAsync` ile ekliyor. `AspNetUserRoles` tablosu ve Identity mimarisi değişmedi — kural yalnızca `UserManagementService` (uygulama) seviyesinde. Create/Edit formlarında checkbox listesi yerine tek-seçim `<select>` dropdown.
    2. **Email/UserName artık değiştirilemez:** Analiz raporunda Email'in Edit formunda düzenlenebilir olması önerilmişti; kullanıcı bunu da değiştirdi — Email aynı zamanda login bilgisi olduğu ve SecurityStamp/NormalizeUserName senkronizasyonu gereksiz karmaşıklık yarattığı gerekçesiyle. `UpdateUserRequest`'ten `Email` alanı tamamen kaldırıldı; `UpdateAsync` artık `SetEmailAsync`/`SetUserNameAsync` çağırmıyor. Edit formunda Email salt-okunur metin olarak gösteriliyor (input olarak render edilmiyor, `UserFormViewModel.Email`'deki `[Required]` attribute'u da bu yüzden kaldırıldı — Create'te zorunluluk artık controller'da manuel kontrol ediliyor, Password alanıyla aynı desen).
    3. Bu 2 revizyon, kod yazılmadan **önce** netleştirildiği için "implementasyon sonrası düzeltme" değil — analiz raporunun implementasyona geçişte son haliyle uygulanmasıdır. Hiçbir yeni migration gerekmedi (AspNetUserRoles ve AspNetUsers.Email/UserName kolonları zaten mevcuttu).
  - **Kritik mimari bulgu — `IUserManagementService` neden interface (projedeki tek örnek):** `Application` projesi `Infrastructure`'a referans veremiyor (mevcut reference graph) ama implementasyon `UserManager<ApplicationUser>`/`RoleManager<IdentityRole>`'a (Infrastructure.Identity) bağımlı olmak zorunda. Çözüm: arayüz + saf primitive DTO'lar (`Application/Users/`) Application'da, implementasyon (`UserManagementService`) Infrastructure'da (`Infrastructure/Identity/`) — projenin her Repository'sinin arayüz-Application/implementasyon-Infrastructure deseninin bir "servis"e ilk uygulanışı, stil tercihi değil katman sınırının zorunlu kıldığı istisna.
  - **Login davranışı:** `AccountController.Login` — `user is not null && user.IsActive` kontrolü `PasswordSignInAsync`'ten ÖNCE yapılıyor (pasif kullanıcı için AccessFailedCount/lockout sayaçları anlamsız yere işlemesin, "kullanıcı yok" ile "kullanıcı pasif" aynı genel hata mesajıyla dışarı yansısın — bilgi sızıntısı yok, mevcut lockout/parola akışı bozulmadı).
  - **Guardrail tasarımı:** `IsLastActiveAdminAsync(user)` tek doğruluk kaynağı (hedef şu an aktif VE Admin VE ondan başka aktif Admin yok → true) — Delete/Deactivate/rol-kaldırma hepsi bunu kullanıyor. Kendi-hesap kontrolü (`user.Id == currentUserId`) üç tehlikeli işlemde ayrı ayrı uygulanıyor. **Servis katmanında** (yalnızca UI'da değil) — controller bypass edilse bile korunuyor.
  - **Rol ataması (revize edilmiş nihai hâl):** `ApplicationRoles.All` (yeni eklenen whitelist array) — serbest metin/yeni rol kabul edilmiyor, her kullanıcı tam olarak bir role sahip (tek-seçim dropdown, `UpdateAsync` rol değiştiğinde eski rolü kaldırıp yenisini ekliyor).
  - **Domain:** Yeni entity yok — `ApplicationUser`'a (Infrastructure.Identity) `IsActive` bool eklendi. `ApplicationUserConfiguration` (`HasDefaultValue(true)` — Dealer/Banner/NewsCategory'nin bool-aktif deseniyle tutarlı).
  - **Application:** `Application/Users/` — `IUserManagementService`, `UserDto`, `CreateUserRequest`/`UpdateUserRequest`/`ResetUserPasswordRequest`, `UserOperationResult` (tümü saf primitive, `ApplicationUser` referansı yok).
  - **Infrastructure:** `Infrastructure/Identity/UserManagementService.cs` — `UserManager`/`RoleManager` doğrudan kullanılıyor, hiçbir noktada `AppDbContext.Users`'a doğrudan yazma yok. Email hem `UserName` hem `Email` olarak atanıyor (yalnızca `CreateAsync`'te — oluşturulduktan sonra hiç değişmiyor), admin-oluşturmalı kullanıcılarda `EmailConfirmed=true`. `UpdateAsync` `SetUserNameAsync`/`SetEmailAsync` **çağırmıyor** (revize karar — Email/UserName immutable).
  - **Presentation:** `UserController` (`[Authorize(Roles=Admin)]` sınıf seviyesi — Dealer'daki salt-Admin desenle birebir), Index/Create/Edit/ResetPassword/Activate/Deactivate/Delete, dar ViewModel'ler (`UserFormViewModel`/`UserIndexViewModel`/`ResetPasswordViewModel` — `ApplicationUser`'a doğrudan bind yok), current user id `User.FindFirstValue(ClaimTypes.NameIdentifier)` ile (controller'a `UserManager` bağımlılığı eklenmedi).
  - **Migration:** `AddIsActiveToApplicationUser` — yalnızca `AspNetUsers.IsActive` kolonu (bit, NOT NULL, DEFAULT 1); mevcut seed edilmiş admin/dev-test kullanıcıları migration sonrası sqlcmd ile `IsActive=1` doğrulandı.
  - **Doğrulama:** Clean build 0 Warning/0 Error; solution toplamı **269/269 test geçti** (148 unit [116+32 yeni] + 121 integration [86+35 yeni]). Coverage: global %15.7 line; `UserManagementService` %88.5, `UserController` %57.6, `AccountController` %60, `ApplicationUser`/`ApplicationRoles` %100. Docker'a hiç dokunulmadı.
  - **Test altyapısı notu:** `UserManagementTests.cs` (integration), `TestAuthHandler`'ın "mevcut kullanıcı"yı sabit `"test-user-id"` claim'iyle temsil etmesi nedeniyle, diğer integration test sınıflarının aksine `IClassFixture<CustomWebApplicationFactory>` **paylaşmıyor** — her test kendi `CustomWebApplicationFactory` örneğini (izole SQLite bağlantısı) oluşturuyor, kendi-hesap guardrail testlerinin gerçek bir `ApplicationUser` kaydı gerektirmesi ve paylaşılan DB'de testler arası Id çakışması riski nedeniyle.
  - Yeni ADR gerekmedi (ADR-005'in "Identity altyapısı sarmalanmayacak" ilkesinin doğal bir uygulaması).
  - Backlog madde #2 ("Kullanıcı/Rol Yönetimi") **CRUD/RBAC kısmı tamamlandı** işaretlendi — Role Management (Task 17'de ele alındı, bkz. aşağısı).
  - **Bilinçli olarak yapılmayanlar:** Dinamik Role Management ekranı, yeni rol oluşturma/silme, claim/permission altyapısı, alan-seviyeli RBAC, 2FA, SMTP/e-posta gönderimi, self-registration, public "şifremi unuttum" akışı, profil fotoğrafı, ad-soyad/departman gibi dokümanda olmayan alanlar, public API, SPA, yeni paket, Docker canlı doğrulaması.

- [x] **Task 17** — Role Management (Backlog #2, kalan kısım) — **TAMAMLANDI (20.07.2026), analiz + aynı oturumda otomatik implementasyon.**
  - **Analiz:** Referans doküman (Madde 7.2/17.2/30/31), Task 16 analiz/kapanış raporu, `ApplicationRoles`/`IdentitySeeder`/`ApplicationUser`/`UserManagementService`/`IUserManagementService`/`UserController`/User View'ları, `Infrastructure/DependencyInjection.cs`, `Program.cs`, tüm `[Authorize(Roles=...)]` kullanımları (her controller tek tek kod taranarak), RBAC integration testleri, `IdentityTestHost`/`CustomWebApplicationFactory`/`TestAuthHandler`/`TestClientExtensions`/`AntiForgeryHelper` okundu.
  - **Kritik bulgu 1:** Madde 17.2'nin 15 modüllük tablosunda "Kullanıcı/Rol Yönetimi" **tek satır** (#14) — ayrı bir "Role Management" modülü doküman düzeyinde hiç yok. Madde 7.2 dört rolü (Admin/İçerik Editörü/SEO Editörü/Ürün Yöneticisi) kapalı bir liste olarak tanımlıyor, dinamik rol oluşturma/silmeye dair sıfır kanıt.
  - **Kritik bulgu 2:** Mevcut RBAC tamamen derleme-zamanı `[Authorize(Roles=...)]`/`ViewRoles`/`EditRoles` const string'lerine dayanıyor (kod taranarak doğrulandı, tam matris kapanış raporunda) — yeni bir rol `RoleManager.CreateAsync` ile oluşturulsa bile hiçbir controller'da otomatik erişim kazanmaz. Bu, dinamik rol CRUD'ı hem dokümansız hem işlevsiz kılıyor (permission/claim sistemi olmadan üretilen roller AspNetRoles'a yazılır ama hiçbir ekranı açmaz).
  - **Kritik bulgu 3:** Seçenek B'nin ("teknik ad vs görünen ad ayrı yönetilsin") öncülü bu sistemde hiç yok — `ApplicationRoles` sabitleri (ör. "İçerik Editörü") zaten hem `IdentityRole.Name` hem `[Authorize(Roles=...)]` string'i, ayrı bir "DisplayName" alanı icat etmek dokümansız+riskli olurdu (rol adı = auth string'i, ayrışma senkronizasyon hatası riski taşır).
  - **Sonuç — kritik mimari/kapsam çelişkisi yok, otomatik implementasyona geçildi (Seçenek A):** Sabit 4 rol için salt-okunur ekran; dinamik rol CRUD/permission/claim kapsam dışı.
  - **Application:** `Application/Roles/` — `IRoleManagementService` (`GetAllAsync`/`GetByNameAsync`), `RoleDto`, `RoleDetailDto`, `RoleUserDto`, `RolePermissionDto` + `RoleAccessLevel` enum (Full/ViewOnly/None/NotImplemented). Hiçbiri `ApplicationUser`/Identity tipine referans vermiyor.
  - **Infrastructure:** `Infrastructure/Identity/RoleManagementService.cs` — `RoleManager<IdentityRole>`/`UserManager<ApplicationUser>` kullanıyor, ADR-016 deseninin (arayüz-Application/implementasyon-Infrastructure) **ikinci** uygulanışı. `ApplicationRoles.All` tek doğruluk kaynağı — `_roleManager.Roles` DOĞRUDAN enumerate edilmiyor, DB'de fazladan/beklenmeyen bir rol olsa bile sistem rolü olarak gösterilmiyor. Yetki matrisi (`PermissionMatrix`) her controller'ın gerçek `ViewRoles`/`EditRoles` sabitlerinden **elle** taranarak çıkarıldı — dinamik/reflection-tabanlı otomatik keşif kasıtlı olarak yapılmadı. Henüz controller'ı olmayan 3 modül (Dil Yönetimi/SEO Yönetimi/Excel Import) "Henüz Uygulanmadı" işaretli, tahmini erişim seviyesi verilmedi. Rol açıklamaları Madde 7.2'nin doğrudan metni (icat edilmedi).
  - **Presentation:** `RoleController` (`[Authorize(Roles=Admin)]` sınıf seviyesi, Dealer/User'daki salt-Admin desenle aynı) — yalnızca `Index`/`Details` GET action'ları (`[HttpGet]` açıkça belirtildi, POST'un yanlışlıkla aynı action'a yönlenmemesi için), hiçbir state-changing action yok. `RoleIndexViewModel`/`RoleDetailViewModel`, `Views/Role/{Index,Details}.cshtml`. `_Layout.cshtml`'e **projedeki ilk rol-koşullu nav linki** eklendi (`@if (User.IsInRole(ApplicationRoles.Admin))` — diğer tüm linkler yalnızca `IsAuthenticated` şartına bağlı, bu görev talimatının açık isteğiydi).
  - **Migration:** Gerekmedi — Identity şemasına hiç dokunulmadı, yetki matrisi kodda statik veri.
  - **Unit testler:** `RoleManagementServiceTests.cs` — 15 test (`IdentityTestHost`, `UserManagementServiceTests` ile aynı desen), talimatın 14 senaryosunun tamamı: 4 sabit rol listeleme, beklenmeyen DB rolünün listelenmemesi, kullanıcı sayıları (aktif/toplam/pasif), atanmış kullanıcı listesi + email sıralaması, bilinmeyen rol→null, yetki matrisi doğruluğu, Kullanıcı/Rol Yönetimi modülünün yalnız Admin'e Full vermesi, tek-rol kuralıyla uyum, boş liste, eksik seed rolünün hatasız işlenmesi.
  - **Integration testler:** `RoleManagementTests.cs` — 13 test (talimatın 12 senaryosu, #12 Theory 2 case): Anonymous Index/Details reddi, Admin Index/Details izni, ContentEditor/SeoEditor/ProductManager/rolsüz reddi, geçersiz rol→404, kullanıcı listesinin render edildiği, nav linkinin yalnız Admin'de göründüğü, `/Role` ve `/Role/Details/Admin`'e POST'un 404/405 döndüğü.
  - **Doğrulama:** Clean build 0 Warning/0 Error; solution toplamı **297/297 test geçti** (163 unit [148+15 yeni] + 134 integration [121+13 yeni]). Coverage: global %16.4 line (Task 16B'nin %15.7'sinden yükseldi); `RoleManagementService` %100, `RoleController` %100, tüm yeni DTO/View'lar %100. Docker'a hiç dokunulmadı.
  - **ADR-016 güncellendi (yeni ADR değil):** "Projedeki tek örnek" ifadesi artık doğru değildi (2. kullanım) — ADR-016'ya bu genişlemeyi kaydeden bir not eklendi, karar metninin kendisi değişmedi.
  - Backlog madde #2 ("Kullanıcı/Rol Yönetimi") **tamamen kapandı** — dinamik rol CRUD dokümanda dayanağı olmadığı için ayrı bir backlog maddesi olarak hiç açılmadı.
  - **Bilinçli olarak yapılmayanlar:** Dinamik rol oluşturma/silme/yeniden adlandırma, permission entity, claim CRUD, alan-seviyeli yetkilendirme, bu ekrandan kullanıcıya rol atama (User Management'ın konusu), User Management davranış değişikliği, `ApplicationRoles` dışındaki rolleri sistem rolü kabul etme, Identity şema değişikliği, yeni migration, yeni paket, public API, SPA, 2FA, SMTP, görsel tasarım çalışması.

- [x] **Task 18** — Dashboard (Backlog #19) — **TAMAMLANDI (20.07.2026), analiz + aynı oturumda otomatik implementasyon.**
  - **Analiz:** PROJECT_MEMORY.md/PROGRESS.md/TASKS.md/ARCHITECTURE_DECISIONS.md/TESTING.md + referans doküman + mevcut Dashboard kodu (`HomeController`, `Home/Index.cshtml`) + 6 entity'nin gerçek alanları (`Product`, `Category`, `Collection`, `Dealer`, `ApplicationUser`, `FormSubmission`) tek tek okundu, hiçbir alan tahmin edilmedi.
  - **Kritik bulgu 1:** Madde 17.2'nin 15 modüllük tablosunda Dashboard yalnızca isim olarak geçiyor (#1) — kart/grafik/tablo tanımı hiç yok.
  - **Kritik bulgu 2:** Mevcut Dashboard (`HomeController.Index`) tamamen boştu — `<h2>Hoş geldiniz, @User.Identity?.Name</h2>`'den ibaret, gerçek veriye bağlı değildi.
  - **Kritik bulgu 3:** `Product`'ta `IsActive` yok, `Status` enum var (`Active/Inactive/InProgress/Cancelled`) — "aktif ürün" = `Status==Active`. `FormSubmission`'da `IsRead` ve `ProcessedAt` **bağımsız iki alan** (Task 15'in Details GET'i bilinçli olarak otomatik-okundu-işaretlemediği için ayrım anlamlı) — "okunmamış/bekleyen" tek bir alana indirgenmedi, iki ayrı sayaç (`UnreadFormSubmissions`+`UnprocessedFormSubmissions`) olarak eklendi (bilgi kaybı yaratmayan çözüm, kullanıcıya soru sormaya gerek kalmadı).
  - **Kritik bulgu 4:** `ApplicationUser`'da `CreatedAt` yok (`IdentityUser` temel sınıfı sağlamıyor) — "son eklenen kullanıcılar" listesi desteklenmiyor, eklenmedi.
  - **Kritik bulgu 5:** Projede hiç CSS framework kurulu değil (`wwwroot` yok, `_Layout.cshtml`'de tek bir `<link>` yok) — talimatın "mevcut Bootstrap'ı kullan" varsayımı gerçekle uyuşmuyordu; mevcut konvansiyon (çıplak semantik HTML) korundu, yeni framework eklenmedi.
  - **Sonuç — kritik mimari/kapsam çelişkisi yok, otomatik implementasyona geçildi:** Salt-okunur özet ekranı; yeni entity/migration/audit sistemi/frontend paketi yok.
  - **Application:** `Application/Dashboard/` — `IDashboardService` (`GetDashboardAsync`), `DashboardDto` (11 sayaç alanı + `RecentProducts`/`RecentForms`), `DashboardRecentProductDto`, `DashboardRecentFormDto`.
  - **Infrastructure:** `Infrastructure/Persistence/DashboardService.cs` — **doğrudan `AppDbContext`'e bağımlı** (mevcut 6 modül repository'sinin hiçbirinde `CountAsync`/predicate yoktu, bunlara yeni metod eklemek yerine ADR-016'nın Identity-özel olmayan genel bir uygulanışı seçildi). Tüm sorgular `AsNoTracking()` + DB-seviyesi `CountAsync()`/`Where().CountAsync()`/`OrderByDescending().Take(5)` — hiçbir tam tablo belleğe çekilmedi, Recent Products/Forms projeksiyonları navigation property kullanmadığı için N+1 yok.
  - **Presentation:** `HomeController.Index` genişletildi (`[HttpGet]`, `[Authorize]` — **rol şartı yok**, Task 2.1'den beri değişmedi, bilinçli olarak korundu), `DashboardViewModel`, `Views/Home/Index.cshtml` — 8 özet kartı + Son 5 Ürün tablosu + Son 5 Form tablosu. "Son Form Başvuruları" bölümü yalnızca `FormSubmissionController.ViewRoles`'e (Admin+İçerik Editörü) sahip roller için render ediliyor (`Product/Index.cshtml`'deki `canEdit` deseniyle aynı yaklaşım — link-güvenliği, "farklı dashboard icat etme" değil). "Son Ürünler" satırlarında Edit linki yalnızca `ProductController.EditRoles`'e (Admin+ProductManager) sahip roller için gösteriliyor.
  - **Migration:** Gerekmedi — yalnızca mevcut verileri okuyor.
  - **Unit testler:** `DashboardServiceTests.cs` — 15 test (`SqliteTestDatabase`, doğrudan `AppDbContext` insert), talimatın 15 senaryosunun tamamı: ürün/kategori/koleksiyon/bayi/showroom/kullanıcı/form sayaçları, aktif-ürün ve aktif-kullanıcı filtreleri, okunmamış/işlenmemiş form filtrelerinin bağımsızlığı, son ürün/form sıralaması + limit, boş veri, kategorisiz Dealer'ın her iki sayaçtan da hariç tutulması (hata vermeden).
  - **Integration testler:** `DashboardTests.cs` — 14 test (talimatın 13 senaryosu + 1 ek): Anonymous reddi, 4 rolün tamamının izni, rolsüz kullanıcının da izni (**not:** talimatın "rolsüz erişemez" varsayımı `HomeController`'ın gerçek `[Authorize]`-only davranışıyla çelişiyordu, koddaki gerçek davranışa göre düzeltildi), render/sayaç/sıralama doğrulaması (izole `CustomWebApplicationFactory`, TESTING.md §7B deseni), POST'un 404/405 dönmesi, SeoEditor'de Form bölümünün gizlenmesi/ContentEditor'de görünmesi.
  - **Doğrulama:** Clean build 0 Warning/0 Error; solution toplamı **326/326 test geçti** (178 unit [163+15 yeni] + 148 integration [134+14 yeni]). Coverage: `DashboardService`/`DashboardDto`/`DashboardRecentProductDto`/`DashboardRecentFormDto`/`DashboardViewModel` %100. Docker'a hiç dokunulmadı.
  - **ADR-016 güncellendi (yeni ADR değil):** Not, artık Identity-özel olmayan `AppDbContext`-doğrudan-bağımlılık örneklerini de kapsayacak şekilde genişletildi.
  - Backlog madde #19 ("Dashboard") **tamamlandı**.
  - **Bilinçli olarak yapılmayanlar:** Audit/activity log, kullanıcı hareket geçmişi, gerçek-zamanlı Dashboard, SignalR, Chart.js/grafik paketi, yapay grafik verisi, yeni entity/tablo/migration, public API, SPA, AJAX otomatik yenileme, bildirim merkezi, export, yeni CSS framework, role göre farklı kart seti (dokümanda/kodda kanıt yok).

- [x] **Task 19** — Tabler UI Entegrasyonu — Faz 1 Prototip (backlog dışı, kullanıcı talebi) — **TAMAMLANDI (20.07.2026).**
  - **Kapsam:** İş modülü değil, salt Presentation/UI katmanı görsel yenilemesi. Kesin kural: Controller/Service/Repository/Entity/Migration/DB şeması/route/form action/`asp-for` model binding/AntiForgery/Validation/RBAC/mevcut testler **hiç değişmedi**.
  - **Önce analiz (kod taranarak, tahmin edilmeden):** `_Layout.cshtml`, `Account/Login.cshtml`, `Home/Index.cshtml`, `Product/Index|Create|Edit.cshtml`, `User/Index.cshtml`, mevcut `site.css` (Task 18 itibarıyla teyit edilmiş: proje hiç CSS framework kullanmıyordu), tüm controller `ViewRoles`/`EditRoles` sabitleri, ve tüm entegrasyon testlerindeki literal string/route assertion'ları (`.Should().Contain(`, `body.Should()`) — kırılma riski taşıyan tüm noktalar implementasyondan önce çıkarıldı.
  - **Varlık kazanımı:** `@tabler/core@1.4.0` + `@tabler/icons-webfont@3.45.0` (ikisi de MIT) `npm pack` ile indirildi; yalnızca derlenmiş `tabler.min.css`/`tabler.min.js`/`tabler-icons.min.css`+font dosyaları `wwwroot/lib/tabler/` ve `wwwroot/lib/tabler-icons/` altına **yerel** kopyalandı (CDN yok, sürüm sabit, `pnpm`/Liquid/demo-backend/demo-veri/önizleme-uygulaması/React-Vue-Angular sürümleri hiç alınmadı). Derlenmiş CSS'te harici `@import`/Google Fonts referansı olmadığı grep ile doğrulandı. Lisans metinleri + kapsam notu yeni `THIRD_PARTY_NOTICES.md`'de.
  - **Marka teması:** `wwwroot/css/site.css` yeniden yazıldı — Tabler'ın kendi dosyasına dokunulmadan, `:root`'ta `--ng-*` CSS değişkenleri (kullanıcının verdiği tam palet: `--ng-bg #f4f2ee`, `--ng-sidebar #202522`, `--ng-accent #a48968` vb.) tanımlanıp Tabler'ın `--tblr-body-bg`/`--tblr-bg-surface`/`--tblr-border-color`/`--tblr-primary`/`--tblr-primary-rgb` token'larına atandı (cascade ile override, sonradan yüklenen kazanır); koyu sidebar için ayrıca `.navbar-vertical[data-bs-theme="dark"]` seçicisi altında `--tblr-navbar-bg`/`--tblr-navbar-active-bg`/vb. ikinci, hedefli bir override bloğu eklendi (Tabler'ın `color-mix()` tabanlı otomatik dark-mode cascade'ine güvenmek yerine piksel-kesin kontrol için).
  - **Layout:** `Views/Shared/_Sidebar.cshtml` (yeni, `navbar-vertical` koyu sidebar, kullanıcının istediği 5 grup: Genel/Ürün Yönetimi/İçerik/Operasyon/Sistem, 14 nav linki) + `_Topbar.cshtml` (yeni, sol: `ViewData["Title"]`, sağ: e-posta+rol rozeti+çıkış) + `_Layout.cshtml` (yeniden yazıldı, authenticated shell sidebar+topbar+page-body sarıyor; anonim istekte shell'siz düz body). Login için ayrı, sidebar'sız `_LoginLayout.cshtml` (yeni, Tabler'ın `page-center` auth deseni) — Login view'ı `Layout = "_LoginLayout"` ile bu şablona geçti.
  - **Menü görünürlüğü:** Her sidebar öğesi ilgili controller'ın **gerçek** `ViewRoles` sabitinden (kod taranarak) `User.IsInRole(...)` ile koşullu render ediliyor (Razor local function `CanView(params string[] roles)`) — **yalnızca UI kozmetiği, backend `[Authorize(Roles=...)]` hiç değişmedi**, talimatın "UI'da gizlemek güvenlik kabul edilmemeli" şartına birebir uyuldu. Henüz implement edilmemiş modüllere link eklenmedi.
  - **Prototip kapsamı (talimatın kendi sınırı, 8 ekran):** `_Layout`, Sidebar, Topbar partial'ları + `Account/Login.cshtml` + `Home/Index.cshtml` (Dashboard — mevcut ViewModel/gerçek veri korunarak stat-grid/card/empty-state/tablo ile restyle edildi, sahte trend/grafik verisi eklenmedi) + `Product/Index|Create|Edit.cshtml` + `Product/_ProductForm.cshtml` (tüm `asp-for`/`asp-items` binding'leri birebir korunarak Tabler card/`row g-3`/`col-md-*` grid'ine taşındı) + `User/Index.cshtml`. Kalan 9 modül (Category/Collection/Document/ReferenceProject/Blog/News/Banner/Page/Dealer/FormSubmission/Role) bilinçli olarak eski çıplak HTML'de bırakıldı — "tüm projeyi tek seferde değiştirme" talimatına uyuldu.
  - **Formlar/validasyon:** `form-control`/`form-select`/`form-label`/`input-group` sınıfları eklendi; `asp-validation-for`/`asp-validation-summary` tag helper'larının ürettiği `field-validation-error`/`validation-summary-errors` sınıfları (Bootstrap'ın `.is-invalid` sibling mekanizmasına hiç bağlanmadan) düz CSS ile stillendirildi — bir ilk denemede yanlışlıkla eklenen icat sınıflar (`d-empty-hide`, `invalid-feedback d-block`) fark edilip kaldırıldı, standart tag-helper konvansiyonuna dönüldü.
  - **Migration:** Yok — DB şemasına hiç dokunulmadı.
  - **Doğrulama:** Clean build 0 Warning/0 Error (bir önceki manuel `dotnet run` sürecinin dosya kilidi nedeniyle alternatif çıktı klasörüyle doğrulandı — kod hatası değildi). Solution toplamı **326/326 test değişmeden geçti** (görsel değişiklik hiçbir test string'ini/route'unu/RBAC davranışını bozmadı). Uygulama gerçek SQL Server verisiyle manuel çalıştırıldı: Login (POST→302→Dashboard), Logout (POST→302→Login), Dashboard (gerçek sayaçlar+boş durumlar), Ürün Yönetimi (boş durum+CTA), Ürün Ekle (7 dil çeviri alanı+antiforgery token doğrulandı), Kullanıcı Yönetimi (gerçek 2 kullanıcı) — hepsi konsol hatası olmadan, tüm Tabler asset'leri 200 dönerek render edildi.
  - Backlog maddesi değil — kullanıcının doğrudan talebiydi, TASKS.md'nin taslak backlog numaralandırmasına eklenmedi.
  - **Bilinçli olarak yapılmayanlar:** Kalan 9 modülün restyle'ı (yaygınlaştırma, kullanıcı onayı sonrası), footer'da zorunlu "Powered by Tabler" metni (talimat açıkça istemiyor), Tabler'ın demo/preview uygulaması, herhangi bir yeni backend/route/entity, dark-mode toggle, RTL desteği.

**Sıradaki iş:** Kullanıcı onayı sonrası Tabler tasarım sisteminin kalan 9 modüle (Category/Collection/Document/ReferenceProject/Blog/News/Banner/Page/Dealer/FormSubmission/Role Index/Create/Edit ekranları) yaygınlaştırılması, veya Faz 1 backlog'undaki bir sonraki iş modülü (SEO veri sözleşmesi #4, alan-seviyeli RBAC task'ı, Excel Import, Docker Desktop çalışır hale geldiğinde canlı `docker compose up` doğrulaması, kalan modüllerin unit testlerinin genişletilmesi, veya public site fazında Form Yönetimi'nin gönderim endpoint'i+e-posta bildirimi) — henüz başlanmadı, onay bekliyor.

---

## Faz 1 — Yönetim Paneli + Backend API (Taslak Backlog)

### ~~İlk Sürüm Backlog Sıralaması (17.07.2026)~~ — **Geçersiz Kılındı**

İlk sıralama SEO'yu tamamen sona, diğer modüllerden bağımsız bir konu olarak koymuştu. Bu yanlıştı: SEO veri sözleşmesi (SeoMeta alan/ilişki deseni) içerik modüllerinin şemasını etkiliyor. Düzeltilmiş sıralama aşağıdadır.

### Düzeltilmiş Backlog Sıralaması (Bağımlılığa Göre)

> Aşağıdaki liste bir **taslak backlog**tur. Her madde, sırası geldiğinde tek bir task olarak kullanıcı onayına sunulacak; henüz hiçbiri başlatılmamıştır.

1. [x] Solution / katmanlı mimari iskeleti (Presentation / Application / Domain / Infrastructure) — **Task 1.1 olarak tamamlandı (17.07.2026), bkz. Faz 0 bölümündeki detay kaydı.**
2. [x] Kullanıcı / Rol Yönetimi (Users, Roles, RBAC) — **TAMAMEN TAMAMLANDI (Task 16/16B Kullanıcı Yönetimi 20.07.2026 + Task 17 Role Management 20.07.2026).** RBAC altyapısı (Task 1/2.1/2.2) + Kullanıcı Yönetimi (liste/oluştur/düzenle/rol ata/aktif-pasif/parola sıfırla/sil, guardrail'lerle) + Role Management (sabit 4 rol için salt-okunur denetim ekranı) tam çalışıyor. **Dinamik rol CRUD/claim/permission altyapısı bilinçli olarak kapsam dışı** — Task 17 analiziyle netleşti: dokümanda hiç dayanağı yok ve mevcut RBAC tamamen derleme-zamanı `[Authorize(Roles=...)]` sabitlerine dayandığından işlevsiz olurdu; bu "başlanmamış" bir madde değil, dokümana göre hiç açılmayacak bir kapsam.
3. [x] Dil Yönetimi altyapısı (Translations — multi-lang alan deseni) — **TAMAMEN TAMAMLANDI.** Şema/implementasyon Task 3.1A+3.1B'de (19.07.2026) bitmişti. Panel modülünün kendi CRUD ekranı **Task 21'de (21.07.2026) tamamlandı**: Index (7 dil listesi) + güvenli Edit (Code salt okunur, Name/DisplayOrder düzenlenebilir, TR'nin IsActive'i kilitli — "Türkçe devre dışı bırakılamaz"). Yeni dil ekleme/silme action'ı hiç yok (dokümanda dayanağı yok, "seed edilmiş diller silinemez" zaten Delete action'ının hiç var olmamasıyla sağlanıyor). Admin-only, RoleManagementService.PermissionMatrix güncellendi. 22 yeni test (9 unit + 13 integration). **ADR-007'nin ertelenen "eksik çeviri raporu" kısmı ayrı bir görevde (21.07.2026) tamamlandı:** `TranslationCoverageService` (Application.Translations) — Dealer hariç 11 EntityType'ın (SeoManagementService'in 7'sinden geniş: Product/Category/Collection/Blog/BlogCategory/News/NewsCategory/Page/PageContentBlock/Banner/ReferenceProject) TÜM Translation alanlarını (yalnızca SEO değil) tarar; her modülün zaten var olan `GetAllAsync()`/`GetTreeAsync()` DTO'larındaki gömülü `Translations` listesini kullanır (ayrı `ITranslationService.GetTranslationsAsync` çağrısına gerek yok). Dil Yönetimi'ne yeni `Report` action'ı: Toplam eksik çeviri + dile göre + modüle göre sayaç + `type`/`languageId` ile filtrelenebilir liste. Dashboard'a eksik çeviri kartı + rapora link eklendi (`DashboardService` artık `TranslationCoverageService`'e bağımlı). **Kesin kural: fallback/başka dilden doldurma YOK, yalnızca tespit ve raporlama** (görev talimatı, ADR-007 ile tutarlı). Migration yok (mevcut Translation tablosu kullanıldı). 24 yeni test (11 unit + 13 integration).
4. [x] **SEO veri sözleşmesi + SEO Yönetimi** — **TAMAMLANDI (Task 22, 21.07.2026).** Mevcut Translation mimarisindeki SeoUrl/MetaTitle/MetaDescription alanları tek doğruluk kaynağı olarak kullanıldı (ikinci bir SEO tablosu oluşturulmadı). `SeoManagementService`, 7 modülün (Product/Category/Collection/Blog/News/Page/ReferenceProject) zaten var olan servislerini + `ITranslationService`'i sarmalayan ince bir cross-cutting katman. **Kritik bulgu:** Banner'ın hiç SEO alanı yok, ReferenceProject yalnızca SeoUrl'e sahip (MetaTitle/MetaDescription yok) — kullanıcının "8 modül" varsayımı koda göre düzeltildi, icat edilmedi. SEO Yönetimi ekranı: içerik türü + kayıt seçimi, dil-başına SEO alanları, Eksik SEO Raporu, Duplicate SEO URL Raporu (kullanıcı onayıyla: EntityType+Dil bazlı, DB constraint değil uygulama-seviyesi uyarı). RBAC: Admin+SEO Editörü tam, diğer roller erişemiyor (kendi modüllerinde Task 20'nin alan-seviyeli RBAC'ı üzerinden zaten erişiyorlar). Public sitemap/robots.txt/redirect/schema **oluşturulmadı** (kapsam dışı, ADR-001/002/009). Migration yok. 12 yeni unit test (11 normalizer + zaten var 1) + 12 integration test. **Takip — Madde 27 yeniden inceleme (21.07.2026):** Kod tabanı dokümanla karşılaştırıldı, 2 somut doğrulama bug'ı bulunup düzeltildi (ikisi de admin-panel kapsamında, migration yok): (1) `SeoController.Edit(GET)`, `Index`'in aksine `IsSupported` kontrolü yapmıyordu — desteklenmeyen bir `type` (ör. Banner) doğrudan URL ile istenirse `GetRecordsAsync`'in `InvalidOperationException`'ı ile 500 veriyordu; artık Index'teki gibi dostane redirect. (2) `SeoManagementService.UpdateAsync` `languageId`'yi hiç doğrulamıyordu — geçersiz Id FK ihlaliyle 500'e, pasif dilin Id'si ise sessiz yazmaya yol açabiliyordu; artık `ProductService.ValidateAsync`'in "aktif dil bulunamadı" ilkesiyle tutarlı bir kontrol var. Duplicate kontrolü ve SeoUrl/MetaTitle/MetaDescription zaten doğruydu, değişmedi. **Dokümanla karşılaştırmada bulunan ama bu takipte KASITLI olarak uygulanmayan gerçek boşluklar** (kullanıcıya raporlandı, backlog'a not düşüldü): Madde 27.1 admin panelinden düzenlenebilir olmasını istediği canonical URL/og:title/og:description/og:image/twitter:card alanları hiç yok (yalnızca title+meta description = MetaTitle/MetaDescription uygulandı); redirect yönetimi (dokümanın 36.1 veri modelinde yalnızca isim geçen, alan listesi hiç verilmeyen "Redirects" tablosu — kesin tanımlı değil, yeni entity kasıtlı olarak oluşturulmadı); sitemap.xml/robots.txt/schema markup üretimi (kasıtlı olarak public-site kapsamı, ADR-001/002/009). 4 yeni integration test.
5. [x] Kategori Yönetimi — **TAMAMLANDI (Task 4.1, 19.07.2026).** Domain/Application/Infrastructure/Presentation uçtan uca, migration uygulandı, doğrulandı.
6. [x] Koleksiyon Yönetimi — **TAMAMLANDI (Task 4.2, 19.07.2026).** Domain/Application/Infrastructure/Presentation uçtan uca, migration uygulandı, doğrulandı. Category ile FK ilişkisi yok (doküman analiziyle netleşti).
7. [~] Ürün Yönetimi — **Çekirdek CRUD TAMAMLANDI (Task 5, 19.07.2026).** Doküman/proje ilişkileri, arama/filtreleme, Excel import ayrı maddelerin konusu (#9/#10/#17). Görsel ilişkisi Task 5.1 ile tamamlandı (#8).
8. [x] Ürün Görselleri (Render/Face/Lifestyle/Texture/Detail) — **TAMAMLANDI (Task 5.1, 19.07.2026).** `ProductImage` + `IFileStorageService`/ADR-013, uçtan uca, migration uygulandı, doğrulandı.
9. [x] Katalog/Doküman Yönetimi — **TAMAMLANDI (Task 6, 19.07.2026).** `Document`+`ProductDocument`/`CollectionDocument` (M2M) + `IFileStorageService` (Task 5.1'den tekrar kullanıldı) + ADR-014, uçtan uca, migration uygulandı, doğrulandı.
10. [x] Referans Proje Yönetimi — **TAMAMLANDI (Task 7, 19.07.2026).** `ReferenceProject`+`ReferenceProjectImage`+`ProductReferenceProject` (M2M) + `IFileStorageService` (üçüncü kez tekrar kullanıldı), uçtan uca, migration uygulandı, doğrulandı.
11. [x] Bayi/Showroom Yönetimi — **TAMAMLANDI (Task 14, 19.07.2026).** `Dealer` (tek entity, nullable `Category` enum), Translation/görsel/storage kullanılmıyor, RBAC Madde 30 satırı literal (Admin-only), uçtan uca, migration uygulandı, doğrulandı.
12. [x] Sayfa Yönetimi — **TAMAMLANDI (Task 11, 19.07.2026).** `Page`+`PageContentBlock` (5 blok tipi: TextImage/FullWidthImage/VideoEmbed/Accordion/Tab), `IFileStorageService` yedinci kez tekrar kullanıldı, `EntityType.Page` ilk kez tüketildi + yeni `EntityType.PageContentBlock` eklendi, uçtan uca, migration uygulandı, doğrulandı. SEO Editörü'nün alan-seviyeli meta-alan düzenleme yetkisi teknik borç olarak açık (bkz. PROGRESS.md).
13. [x] Banner Yönetimi — **TAMAMLANDI (Task 10, 19.07.2026).** `Banner` (tekil), `IFileStorageService` altıncı kez tekrar kullanıldı, `EntityType.Banner` ilk kez tüketildi, uçtan uca, migration uygulandı, doğrulandı.
14. [x] Blog Yönetimi — **TAMAMLANDI (Task 8, 19.07.2026).** `Blog`+`BlogCategory`+`Tag`+`BlogTag` (M2M), `IFileStorageService` dördüncü kez tekrar kullanıldı, `EntityType.BlogCategory` eklendi, uçtan uca, migration uygulandı, doğrulandı.
15. [x] Haber Yönetimi — **TAMAMLANDI (Task 9, 19.07.2026).** `News`+`NewsCategory`, `IFileStorageService` beşinci kez tekrar kullanıldı, `EntityType.NewsCategory` eklendi, uçtan uca, migration uygulandı, doğrulandı.
16. [x] Form Yönetimi — **TAMAMLANDI (Task 15, 20.07.2026).** `FormSubmission` (tek entity, FormType discriminator), gerçek SQL pagination/filtreleme (ADR-015), RBAC Görüntüleme/Tam ayrımı, uçtan uca, migration uygulandı, doğrulandı. Public gönderim endpoint'i + e-posta bildirimi bilinçli olarak kapsam dışı (public site fazı).
17. [x] Excel Import (yalnızca Ürün Yönetimi) — **TAMAMLANDI (Task 23, 21.07.2026).** Madde 37.2'nin 11 sütunu (ProductCode/ProductName/Series/Status/Size/Surface/Color zorunlu + Category/UsageArea/ApplicationArea/FaceCount opsiyonel) birebir uygulandı — dokümanda olmayan hiçbir sütun eklenmedi. Kullanıcı onayıyla: Excel'den oluşturulan/güncellenen ürünler için Unit/Thickness/BodyType/TR ShortDescription geçici olarak zorunlu değil (manuel formda hiç değişmedi) — bu yüzden `ProductImportService`, `ProductService.CreateAsync/UpdateAsync`'i çağırmaz, `IProductRepository`/`ITranslationService` üzerinden doğrudan yazar. Akış: Şablon indir → Yükle (uzantı/MIME/magic-byte/boyut) → Ön izleme (satır bazlı hata raporu) → Onayla (transaction, "hatalı dosyada kısmi kayıt bırakma") → Sonuç raporu (Toplam/Başarılı/Hatalı/Yeni/Güncellenen/Atlanan). ClosedXML (MIT) eklendi, THIRD_PARTY_NOTICES.md güncellendi. Category/Collection bulunamazsa **otomatik oluşturulmaz** (satır reddedilir). `IUnitOfWork`'e `BeginTransactionAsync` + `IFileStorageService`'e `OpenReadAsync` eklendi (ikisi de additive, mevcut kullanımlar etkilenmedi). RBAC: Admin+Ürün Yöneticisi. Migration yok. 14 integration test.
   - **Takip — Madde 37.3 Import Sonrası Raporlama (Task 24, 21.07.2026):** İlk sürümde eksik olan raporlama katmanı tamamlandı — Hatalı Kayıt/Eksik Zorunlu Alan/Pasif Ürün/Yeni Eklenen/Güncellenen raporları (sekmeli, tek tablo üzerinden filtrelenmiş görünümler) artık `/ProductImport/Result` ekranında görüntülenebiliyor ve CSV olarak indirilebiliyor (server-side üretim, OWASP CSV injection koruması — `=,+,-,@` ile başlayan hücreler tek tırnakla zorlanıyor). Eşleşmeyen görsel raporu **uygulanmadı** — Excel Import'ta hiç görsel eşleştirme altyapısı yok (Madde 37.4 dokümanın kendisinde "Karar Bekleniyor"), bilinçli teknik borç olarak bırakıldı. Mevcut import pipeline'ı (`ProductImportService.ImportAsync`) değişmedi — yalnızca transaction sonucunu dışarı açan tek bir `TransactionRolledBack` bayrağı eklendi (rollback olduğunda parse-zamanında geçerli görünen satırların bile rapora "Hatalı" yansıması için); rapor, `IProductImportFileReader` gibi mevcut import pipeline'ından ayrı, yeni `ProductImportReportBuilder`/`ProductImportCsvWriter` (saf, DB'siz) tarafından üretiliyor ve `IFileStorageService` üzerinden (yeni migration yok) JSON olarak kalıcı saklanıyor. RBAC değişmedi (Admin+Ürün Yöneticisi). 18 yeni unit test (rapor sınıflandırma + CSV injection) + 6 yeni integration test.
18. [ ] **SEO Yönetim Modülü (UI)** — meta alanları düzenleme, redirect yönetimi, sitemap/robots.txt — içerik modülleri (5-16) tamamlandıktan sonra, çünkü meta alanları gerçek içerikle test edilmeli.
19. [x] Dashboard — **TAMAMLANDI (Task 18, 20.07.2026).** 8 özet kartı + Son 5 Ürün/Form, `IDashboardService`, doğrulandı.
20. [ ] Güvenlik sertleştirme (teknik yaklaşım ADR-005/ADR-011 ile netleşti — Rate Limiting/Loglama/Dosya Doğrulama; gerçek limit değerleri ve log hedefleri bu task'ta belirlenecek)
21. [~] Test senaryolarının panel/API tarafına uygulanması — **Genişletiliyor (Task 12, 19.07.2026 + Task 13, 19.07.2026).** Kalıcı test altyapısı (xUnit/FluentAssertions/Moq/SQLite/`WebApplicationFactory`) Task 12'de kuruldu (Blog/Page/PageContentBlock/Product + seeder'lar), Task 13 News/NewsCategory'yi ekledi. Solution toplamı 131 test (75 unit + 56 integration). Kalan 5 modülün (Category/Collection/Document/ReferenceProject/Banner) unit testleri henüz yazılmadı — mevcut altyapı doğrudan tekrar kullanılarak genişletilebilir, madde tamamen kapatılmadı.
22. [ ] Eğitim materyali ve yayına alma hazırlığı (panel kapsamındaki maddeler)
23. [x] **Alan-seviyeli RBAC altyapısı** — **TAMAMLANDI (Task 20, 21.07.2026).** Ürün Yönetimi: İçerik Editörü (Name/ShortDescription/LongDescription) + SEO Editörü (SeoUrl/MetaTitle/MetaDescription) artık Edit'e erişebiliyor, native alanlar salt-okunur. Sayfa Yönetimi: SEO Editörü artık Edit'e erişebiliyor (yalnızca SEO alanları), Title salt-okunur. Kontrol hem View'da (disabled fieldset/input) hem Controller'da (`RestrictToPermittedFields` — DB'den taze okunan mevcut değerle izinsiz alanları geri yazar, overposting'e kapalı) uygulandı — Service/Domain'e dokunulmadı. `RoleAccessLevel.PartialFields` eklendi, `RoleManagementService.PermissionMatrix` güncellendi. 14 yeni test (11 integration + 3 unit), toplam 340/340.

---

## Faz 2 (Kapsam Dışı — Bu Teslimatta Yok, Referans İçin)

- SAP API Entegrasyonu (senkronizasyon) — ICD sonrası
- CRM Entegrasyonu
- SEO İyileştirmeleri (Adgrey önerileri sonrası)

## Faz 3 / Opsiyonel (Kapsam Dışı — Referans İçin)

- 3D Room Visualizer, AI Ürün Arama, AI Chatbot, Mimar Deneyim Platformu, Bayi/B2B Portalı (tamamen kapsam dışı)

## Gelecek Faz (Bu Teslimatın Dışı)

- **Public Web Sitesi** — tüm view'lar, public endpoint'ler, form gönderim (submission) API'si. Şu anda public site kodu, public endpoint tasarımı/isimlendirmesi veya "ileride kullanılır" diye herhangi bir kod ÜRETİLMEYECEK. (bkz. ARCHITECTURE_DECISIONS.md ADR-001)
